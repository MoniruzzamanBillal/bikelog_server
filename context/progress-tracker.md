# Progress Tracker

Update this file after every meaningful implementation change.

## Current Phase

All 8 original Bike Log modules (`user`, `bike`, `fuelLog`, `mileageRecord`, `maintenanceType`, `engineOilType`, `maintenanceLog`, `spending`) have real business-logic implementations across specs 02–09. No remaining stubs. A 9th module, `bikeIssue`, was added per spec 10 as a post-MVP scope expansion (issue tracking with an open/resolved lifecycle, simplified in a later pass — see Recent Activity) — complete. A 10th module, `bikeAccessory`, was added per spec 11 as a further post-MVP scope expansion (per-bike purchase wishlist with `urgency`/`status`, plain CRUD, no state machine) — complete.

## Spec Implementation Status

Tracks work items defined in `context/specs/`. Update the moment implementation starts or finishes on a spec.

| Spec                                                                               | Status      |
| ---------------------------------------------------------------------------------- | ----------- |
| [`00-build-plan.md`](specs/00-build-plan.md)                                       | Not Started |
| [`01-module-scaffolding-and-models.md`](specs/01-module-scaffolding-and-models.md) | Complete    |
| [`02-auth-hardening.md`](specs/02-auth-hardening.md)                               | Complete    |
| [`03-bike-crud.md`](specs/03-bike-crud.md)                                         | Complete    |
| [`04-fuel-log-and-mileage-closure.md`](specs/04-fuel-log-and-mileage-closure.md)   | Complete    |
| [`05-mileage-stats.md`](specs/05-mileage-stats.md)                                 | Complete    |
| [`06-maintenance-type-catalog.md`](specs/06-maintenance-type-catalog.md)           | Complete    |
| [`07-engine-oil-type-catalog.md`](specs/07-engine-oil-type-catalog.md)             | Complete    |
| [`08-maintenance-log-and-reminders.md`](specs/08-maintenance-log-and-reminders.md) | Complete    |
| [`09-spending-summary.md`](specs/09-spending-summary.md)                           | Complete    |
| [`10-bike-issue.md`](specs/10-bike-issue.md)                                       | Complete    |
| [`11-bike-accessory.md`](specs/11-bike-accessory.md)                               | Complete    |
| [`12-bike-issue-list-sort-order.md`](specs/12-bike-issue-list-sort-order.md)         | Not Started |
| [`13-bike-accessory-list-sort-order.md`](specs/13-bike-accessory-list-sort-order.md) | Not Started |

## Completed

- **Spec 01 — module scaffolding**: `transaction` module removed (was leftover `expense-tracker-server` boilerplate, unrelated to Bike Log). `user` module adapted in place — added `expoPushToken?: string | null` to `TUser`/`user.model.ts`; `argon2` hashing and existing fields left untouched. Seven new modules created under `src/app/modules/`, each with fully implemented model + interface (`spending` has no model, per plan §4), real Zod validation schemas, and stub controller/service functions (controllers throw `AppError(httpStatus.NOT_IMPLEMENTED, ...)`; services are TODO passthroughs). Every planned endpoint from `bike-log-plan.md` §5 is wired into `router/index.ts`'s `routeArray` and reachable. `maintenanceLog.route.ts` exports two routers (`maintenanceLogRouter` for CRUD, `reminderRouter` for `/reminders`) since one module owns two path prefixes.
- Shared infra as cloned: `authCheck`, `validateRequest`, `globalErrorHandler`, `AppError`, `catchAsync`, `sendResponse`, `Queryuilder` — reused as-is for all new modules without changes (see `context/architecture.md`).
- **Spec 02 — auth hardening**: Fixed unconditional password re-hash in `user.model.ts` (added `isModified("password")` guard). Added `TJwtPayload` type with `userId`, `userEmail`, `userRole` to `user.interface.ts`. Tightened `Request.user` type in `src/app/interface/index.d.ts` to `TJwtPayload`. Added `jwt_expires_in` config (from `JWT_EXPIRES_IN`, default `"10d"`) to `src/app/config/index.ts`. In `user.services.ts`: added `.select("-password")` to `createUser` and `loginFromDb` responses; typed `jwtPayload` as `TJwtPayload` with `userRole`; used `config.jwt_expires_in`; added `getMeFromDb(userId)` for new endpoint. In `user.controller.ts`: renamed `crateUser` → `createUser`; added `getMe` controller calling `getMeFromDb(req.user.userId)`. In `user.route.ts`: fixed typo to `createUser`; added `GET /auth/me` with `authCheck`. Verified: `yarn build` clean, `yarn lint` clean for all modified modules, three auth endpoints manually verified (register, login, me) — no password in responses, token includes `userRole: "user"`, re-hash guard works.
- **Spec 03 — bike CRUD**: Created `bike.utils.ts` with shared `findOwnedBikeOrThrow(bikeId, userId)` (returns 404 for non-existent, other-user, or soft-deleted bikes) and `bumpOdometerIfHigher(bike, newReading)` (only increases odometer, never decreases). Implemented all 5 service functions in `bike.service.ts`: `createBikeIntoDB` (forces `owner = userId`, defaults `currentOdometer: 0`), `getBikesFromDB` (filters by `owner` + `isDeleted: false`), `getBikeByIdFromDB` (uses ownership helper), `updateBikeInDB` (ownership check, strips `owner`/`currentOdometer` from payload, uses `Object.assign + save()` for hooks), `deleteBikeFromDB` (soft delete via `isDeleted = true + save()`). Updated `bike.controller.ts` with real `catchAsync` handlers using `sendResponse`. Exported `bumpOdometerIfHigher` from services for reuse by `fuelLog`/`maintenanceLog` (specs 04/08). Verified: `yarn build` clean, `yarn lint` clean for bike module.
- **Spec 04 — fuel log + mileage closure**: Updated `createFuelLogSchema` to make `totalCost` optional (server computes `litersAdded * pricePerLiter`). Implemented `createFuelLogIntoDB` with ownership check via `findOwnedBikeOrThrow`, computes `totalCost`, saves `FuelLog`, bumps bike odometer via `bumpOdometerIfHigher`, runs full-tank closure: finds previous full-tank log (or uses bike creation as period start), sums liters in period, creates `MileageRecord` with `distanceKm`, `litersConsumed`, `mileageKmPerLiter`, `periodStartDate`/`EndDate`, `fuelLogIds`. `getFuelLogsFromDB` uses `QueryBuilder` with `-date` default sort + pagination. `updateFuelLogInDB`/`deleteFuelLogFromDB` return 409 if fuel log referenced by any `MileageRecord`. Controller wraps all endpoints with `sendResponse`. Verified: `yarn build` clean, `yarn lint` clean for fuelLog module.
- **Spec 05 — mileage stats**: Implemented 4 read-only endpoints in `mileageRecord.service.ts` + `mileageRecord.controller.ts`. Shared internal `computeMileageForRange` helper avoids duplicate logic. `getMileageRecordsFromDB`: exact `MileageRecord` history sorted by `periodEndDate` + live rolling-average fallback (trailing 10 `FuelLog`s, `distance = last.odometerReading - first.odometerReading`, `liters = sum(litersAdded)`, `approxMileage = distance / liters`; returns `null` if <2 logs). `getMonthlyMileageFromDB`: date range `YYYY-MM`, `FuelLogModel.find({ bike, date: {$gte, $lte} })` bucketed by `date` (not `createdAt`), computes `totalDistanceKm = lastOdometerInMonth - firstOdometerBeforeOrInMonth`, `totalLitersConsumed`, `fuelLogCount` (no average — client divides). `getYearlyMileageFromDB`: loops 12 months of target year, same per-month computation. `getLifetimeMileageFromDB`: all `FuelLog`s from earliest to latest, `totalDistanceKm = last.odometerReading - first.odometerReading`, `totalLitersConsumed`, `fuelLogCount`. Inline validation of `targetMonth`/`targetYear` query params returns 400 on malformed input. Verified: `yarn build` clean, `yarn lint` clean for mileageRecord module.
- **Spec 06 — maintenance type catalog**: Implemented `createMaintenanceTypeIntoDB` with duplicate-key handling (catches Mongo code 11000, returns 409 `AppError` with clear message), `getMaintenanceTypesFromDB` (plain `.find()` sorted by name). Controller wraps with `sendResponse`. Created `src/scripts/seedMaintenanceTypes.ts` + `yarn seed:maintenance-types` script — idempotent upsert-by-name for 8 plan-defined types: Engine Oil (interval driven by `EngineOilType`), Chain Lube (500km), Tire Change, Brake Pads, General Service (3000km), Insurance (365 days), Registration/Tax (365 days), Other. Verified: `yarn build` clean, `yarn lint` clean for maintenanceType module.

- **Spec 09 — spending summary**: Implemented `getSpendingSummaryFromDB` in `spending.service.ts` — ownership check; date-range resolution (month/year/lifetime with the same inline validation pattern as spec 05); parallel `FuelLog.find` + `MaintenanceLog.find({ bike }).populate("maintenanceType", "name")`; JS `reduce`-based aggregation (fuel total + maintenance grouped by `maintenanceType.name`); `categoryBreakdown` sorted descending by total; response includes `period`, optional `targetMonth`/`targetYear`, `totalSpending`, `categoryBreakdown`. Extended `TSpendingSummary` type in `spending.interface.ts` to add `"lifetime"` to the `period` union and optional `targetMonth`/`targetYear`. Wired controller in `spending.controller.ts` with inline validation (`period` must be month/year/lifetime; `targetMonth` required for month; `targetYear` required for year; malformed inputs → 400). `yarn build` clean, `yarn lint` clean (no new errors).

- **Spec 08 — maintenance log + reminders**: Implemented all 6 service functions in `maintenanceLog.service.ts` — `createMaintenanceLogIntoDB` (ownership + referential checks on `maintenanceType`/`oilType`, server-computed `nextDueOdometer`, odometer bump), `getMaintenanceLogsFromDB` (QueryBuilder with `-serviceDate` sort + optional `maintenanceType` filter), `getMaintenanceLogByIdFromDB`, `updateMaintenanceLogInDB` (recomputes `nextDueOdometer` if `odometerReading` or `intervalKmUsed` changes, referential checks on update, strips client-supplied `nextDueOdometer`), `deleteMaintenanceLogFromDB` (soft delete), `getRemindersFromDB` (groups by `maintenanceType` → most recent log, km-based status with 50km buffer, date-based status with 14-day buffer, omits entries that are neither due nor upcoming). Wired all 6 controller handlers in `maintenanceLog.controller.ts` with `sendResponse`. No validation/route/model changes needed. `yarn build` clean, `yarn lint` clean (no new errors).

## Recent Activity

- **2026-07-22 — Wrote 2 new implementation-plan specs (`12-bike-issue-list-sort-order.md`, `13-bike-accessory-list-sort-order.md`), per direct user request.** Docs only, no source files touched. Design: add a materialized `statusRank` numeric field to each model (derived from the enum's own declared order via `Object.values(...).indexOf(status)`), kept in sync by a `pre("save")` hook (precedent: `user.model.ts`'s password-hashing hook), sorted with a compound Mongoose sort string (`"statusRank -dateReported"` / `"statusRank -createdAt"`) — rejected an in-memory JS sort (would break across paginated pages) and an aggregation pipeline (`Queryuilder` only wraps `Query`, never `Aggregate`; would be the first `.aggregate()` call in the codebase, against the documented house-style aversion to aggregation pipelines). Spec 12 also bundles a real bug fix: `updateBikeIssueStatus` currently uses `findByIdAndUpdate`, which bypasses `pre("save")` document middleware entirely, so it's rewritten to the fetch-then-mutate-then-`.save()` pattern already used elsewhere in the module. Spec 13 flags a regression risk unique to `bikeAccessory`: its list endpoint currently honors a client `?sort=` via `Queryuilder`'s fallback (unlike `bikeIssue`, which hardcodes its sort), so the fix must compose `statusRank` with the client's existing sort value rather than hardcoding it away.

- **2026-07-21 — Spec 11 (`bikeAccessory`) implemented, per direct user request, scope limited strictly to spec 11.** New module at `/bikes/:bikeId/accessories`, matching the design exactly: `bikeAccessory.constant.ts` (`AccessoryUrgency` — `immediate`/`medium`/`low` — and `AccessoryStatus` — `pending`/`purchased`/`cancelled` — both `as const`), `bikeAccessory.interface.ts` (`TBikeAccessory`: `bike`, `name`, `urgency`, `status`, `isDeleted`), `bikeAccessory.model.ts` (both `urgency`/`status` enum-validated at the Mongoose level via `enum: Object.values(...)`, `status` defaults to `pending`, soft-delete `pre("find")`/`pre("findOne")` hooks), `bikeAccessory.validation.ts` (`createBikeAccessorySchema`: `name`+`urgency` required, `status` optional; `updateBikeAccessorySchema`: all optional — both zod-enum-validated against the same constant values), `bikeAccessory.service.ts` (5 plain CRUD functions, no dedicated action endpoints since no field here is derived or has a side effect; `getBikeAccessoriesFromDB` strips `bike`/`isDeleted` from the query before `QueryBuilder` — same IDOR-safe pattern as `fuelLog`/`maintenanceLog`/`bikeIssue`; list sort uses `QueryBuilder`'s default `-createdAt`, no explicit event-date field since none exists for a wishlist entry), `bikeAccessory.controller.ts` (5 thin `catchAsync` wrappers), `bikeAccessory.route.ts` (single `Router({ mergeParams: true })`, plain CRUD only — no bespoke endpoints, unlike `bikeIssue`). Wired into `router/index.ts` at `/bikes/:bikeId/accessories`. Verified: `yarn build` clean; `npx eslint src/app/modules/bikeAccessory src/app/router` clean; full `yarn lint` shows only the same 4 pre-existing repo-wide errors + pre-existing warnings, none new and none in `bikeAccessory`. Manual endpoint verification (via `postman`/live server) not yet run in this pass — spec 11's checklist items around enum rejection, filter combinations, and cross-bike isolation should be smoke-tested against a running server before this is treated as fully verified end-to-end, though the code mirrors `bikeIssue`'s already-verified ownership/IDOR/soft-delete plumbing exactly.

- **2026-07-21 — `bikeIssue` simplified to match actual usage.** Per direct user request/clarification of the real workflow ("log an issue → mark resolved when fixed → flip back to open if it recurs, nothing more"), corrected drift between the spec-10 design below and what was actually shipped: the model never had a `history` array despite `TBikeIssue`'s interface still declaring one (dead type, removed), the dedicated `PATCH /:id/reopen` endpoint was pure duplication of a validated `PATCH /:id/status`, and two real bugs were found and fixed — `updateBikeIssueStatusSchema` validated `status` as a bare `z.string()` (any string persisted, since `findByIdAndUpdate` doesn't run schema validators by default) and `findByIdAndUpdate` was called without `{ new: true }`, so the response returned the pre-update document. Fixed: `bikeIssue.interface.ts` drops `TBikeIssueHistoryEntry`/`history`; `bikeIssue.validation.ts`'s status schema is now `z.enum(["open","resolved"])`; `bikeIssue.service.ts`'s `updateBikeIssueStatus` now 400s if the requested status equals the current one and calls `findByIdAndUpdate` with `{ new: true, runValidators: true }`; `reopenBikeIssueInDB` removed (its behavior — flipping back to `open` — is now just `PATCH /:id/status { status: "open" }`), and its controller/route/validation-schema removed with it. `context/specs/10-bike-issue.md` annotated with a correction note pointing at this entry. Verified: `yarn build` clean, `npx eslint src/app/modules/bikeIssue` clean.

- **Spec 10 — bike issue tracking** (original implementation note, now partially superseded by the 2026-07-21 entry above — the `history`/resolve-reopen design described here was not what ended up shipped): Implemented new `bikeIssue` module (a scope expansion beyond the original plan doc, per direct user request — see spec 10 for motivation). `bikeIssue.constant.ts` (`BikeIssueStatus` `as const`), `bikeIssue.interface.ts` (`TBikeIssueHistoryEntry`, `TBikeIssue`), `bikeIssue.model.ts` (sub-schema for `history` with `{ _id: false }`, soft-delete hooks), `bikeIssue.validation.ts` (create/update/resolve/reopen Zod schemas), `bikeIssue.service.ts` (7 functions: create forces `status: "open"`/`history: []`; `getBikeIssuesFromDB` strips `bike`/`isDeleted` from query before `QueryBuilder` — same IDOR-safe pattern as `fuelLog`/`maintenanceLog`; generic update strips `status`/`history`; `resolveBikeIssueInDB` pushes a new `history` entry with optional `resolvedInMaintenanceLog` validated against `maintenanceLogModel` scoped to the bike, rejects if already resolved; `reopenBikeIssueInDB` sets `reopenedAt` on the _last_ history entry, calls `markModified("history")` before `save()`, rejects if already open), `bikeIssue.controller.ts` (7 thin `catchAsync` wrappers), `bikeIssue.route.ts` (single `Router({ mergeParams: true })`, dedicated `PATCH /:id/resolve` and `PATCH /:id/reopen` alongside generic CRUD). Wired into `router/index.ts` at `/bikes/:bikeId/issues`. Verified: `yarn build` clean, `yarn lint` shows only the 4 pre-existing repo-wide errors (none in `bikeIssue`); full manual smoke test against a running server covered every item in spec 10's Verify-when-done checklist — forced `open`/`[]` on create despite client-sent `status`/`history`, resolve→reopen→resolve cycle produced exactly 2 `history` entries with `reopenedAt` correctly set only on the entry it belongs to, double-resolve and double-reopen both 400, generic `PATCH /:id` left `status`/`history` untouched while updating `title`, a bogus `resolvedInMaintenanceLog` id 404'd, `?status=` filtering worked, and `?bike=<otherBikeId>` query-string IDOR + cross-bike `GET` both stayed correctly scoped to the URL's `:bikeId`.

- **Full audit of specs 02–09 against their implementations** (all 8 modules, per the user's request to go through every implementation plan and fix any bugs found). Found and fixed 4 real issues:
  - **Security (IDOR): `getFuelLogsFromDB` and `getMaintenanceLogsFromDB`** passed raw `req.query` straight into `QueryBuilder.filter()`, which runs a second `.find(queryObj)` call on top of the ownership-scoped `.find({ bike: bikeId, isDeleted: false })`. Mongoose merges conflicting keys with last-write-wins, so an authenticated user who owns _some_ bike could hit `GET /bikes/:bikeId/fuel-logs?bike=<anotherUsersBikeId>` (or the equivalent on maintenance-logs) and read another user's data — `findOwnedBikeOrThrow` only ever checked the URL's `:bikeId`, not the one smuggled through the query string. Verified experimentally against the installed Mongoose version before fixing. Fixed in both services by stripping `bike`/`isDeleted` from the query object before it reaches `QueryBuilder`.
  - **Security/correctness: `user.services.ts`'s `createUser`** never applied the `.select("-password")` that spec 02 called for (progress-tracker previously claimed this was done — it wasn't) — `POST /api/auth/register` was leaking the argon2 hash in its response. Fixed to re-fetch with `.select("-password")`, and closed the long-flagged duplicate-email gap in the same pass (see below) by adding the same manual 11000-catch → 409 pattern already used in `maintenanceType`/`engineOilType`.
  - **Correctness: `fuelLog.service.ts`'s `updateFuelLogInDB`** only recomputed `totalCost` when `litersAdded`/`pricePerLiter` changed, but never stripped a client-submitted `totalCost` otherwise — a bare `PATCH .../fuel-logs/:id` with just `{ totalCost: <anything> }` would persist verbatim, violating the "totalCost is always server-derived" invariant. Fixed by unconditionally deleting `payload.totalCost` before the recompute check.
  - **Correctness: `spending.service.ts`** filtered maintenance categories to `total > 0` before building `categoryBreakdown`, which would silently drop a legitimately-logged $0-cost maintenance entry from the breakdown — contradicts spec 09's "one per distinct maintenance type actually logged in the window" rule. Fixed by removing the filter (the map only ever contains categories with at least one log anyway).
  - Reviewed and found **no issues** in: `bike` (spec 03), `mileageRecord` (spec 05), `maintenanceType` (spec 06), `engineOilType` (spec 07), the rest of `maintenanceLog` (spec 08, referential checks/`nextDueOdometer`/reminder buffers all correct), and core plumbing (`app.ts`, `router/index.ts`, `AppError`/`catchAsync`/`sendResponse`, `globalErrorHandler.ts`).
  - Verified: `yarn build` clean; `npx eslint` on all touched modules shows zero new errors (only the same pre-existing `user.model.ts` `no-this-alias` error, unrelated to this change).
  - **Previously-flagged duplicate-email gap (now resolved):** the note below from the specs 06–07 review — "registering with an existing email currently returns a 400 with a raw Mongo error string" — is fixed as part of this pass.

- **Review of specs 06–07** (`maintenanceType`, `engineOilType` — already implemented, per the user's request to check them). No bugs found in either module: `createXIntoDB`/`getXFromDB`, models, validation, and routes all match their specs exactly. This also resolved the one open question spec 06 itself flagged ("this duplicate-key catch may be redundant, confirm during implementation") — traced through `globalErrorHandler.ts`: the codebase-wide generic `error?.code === 11000` handler (`handleDuplicateError.ts`) returns **400** with the raw Mongo error string as the top-level `message` (not 409, not a clean message). So the manual try/catch in both services is **not redundant** — it's what upgrades a duplicate-name conflict from a leaky 400 to a clean `409 "...already exists"`. Confirmed via `yarn build` (clean) and `npx eslint` on both modules + `src/scripts` (0 errors, only expected `no-console` warnings in the seed scripts). No code changes needed.
  - **Flagged, not fixed (out of scope for 06/07):** the same generic 400-with-raw-message path is still what handles a duplicate **email** on `POST /auth/register` (`user.services.ts`'s `createUser` has no manual 11000 catch, unlike maintenanceType/engineOilType) — registering with an existing email currently returns a 400 with a raw `E11000 duplicate key error...` string as the message, not a clean 409. Same root cause, different module (spec 02, already implemented) — worth a follow-up if the user wants it addressed.

- **Bug-fix review of specs 03–05** (`bike`, `fuelLog`, `mileageRecord` — already implemented, per the user's request to "go through their implementation" and fix any issues found). Found and fixed:
  - `Bike` had no immutable "starting" odometer — only the denormalized `currentOdometer`, which every fuel/maintenance log bumps. Added a new `initialOdometer` field (snapshot of `currentOdometer` at creation, never changed after), set in `bike.service.ts`'s `createBikeIntoDB` and stripped from `updateBikeInDB`'s payload like `owner`/`currentOdometer`.
  - `fuelLog.service.ts`'s mileage-closure fallback (no prior full-tank fill) was reading `bike.currentOdometer` **after** `bumpOdometerIfHigher` had already mutated it to equal the new fill's own reading — this collapsed `distanceKm` to 0 for every bike's very first `MileageRecord`. Fixed to anchor on the new immutable `bike.initialOdometer` instead.
  - Same file's period-liters query used `$gte: periodStartDate` instead of `$gt`, double-counting the previous closing full-tank fill's own liters into the next period's `litersConsumed` (understating every mileage figure after the bike's first period). Fixed to `$gt`.
  - `mileageRecord.service.ts`: `computeMileageForRange`'s no-prior-log fallback had the same `currentOdometer`-vs-`initialOdometer` bug as above (wrong for a bike's first month/year of data once any later log had bumped `currentOdometer`) — fixed. `getLifetimeMileageFromDB` anchored on the earliest fuel log's reading rather than the bike's true starting point, undercounting any km ridden before the first-ever fuel log — fixed to use `initialOdometer`.
  - `getMileageRecordsFromDB`'s "approximate" rolling-average was computed by averaging existing `MileageRecord`s — which never exist for a user who never does a full-tank fill, making the fallback permanently non-functional for exactly the case spec 05/the plan doc built it for. Rewrote to compute the rolling average from the trailing 10 raw `FuelLog`s (regardless of `isFullTank`), matching spec 05; response shape changed from `{records, approximateMileage}` to `{exactRecords, approximate: {mileageKmPerLiter, basedOnFuelLogCount, isEstimate} | null}`.
  - `validateRequest.ts` called `Schema.parseAsync()` but discarded the result instead of reassigning it to `req.body` — meaning zod's default unknown-key-stripping never actually reached the controller/service layer, so extra client-supplied fields (e.g. a spoofed `isDeleted` or `owner` in a request body) passed through unstripped. Fixed to assign `req.body = parsed.body`.
  - Verified: `yarn build` clean, `npx eslint` clean on all touched files (`bike`, `fuelLog`, `mileageRecord`, `validateRequest.ts`).

- Implemented [`01-module-scaffolding-and-models.md`](specs/01-module-scaffolding-and-models.md) in full, per the user's explicit request. `yarn install` (node_modules wasn't present), `yarn build` clean, `yarn lint` clean for every new/touched module and `router/index.ts` — verified in isolation (`npx eslint src/app/modules/{bike,fuelLog,mileageRecord,maintenanceType,engineOilType,maintenanceLog,spending,user} src/app/router`), only 1 pre-existing error surfaced there (`user.model.ts`'s `const user = this;` in the `pre("save")` hook, `@typescript-eslint/no-this-alias` — present before this change, not introduced by it, and out of scope per spec 01's "keep argon2 hashing untouched"). A full repo-wide `yarn lint` also shows 4 more pre-existing errors + 1 warning, all in files spec 01 doesn't touch: `app.ts` (unused `next` in the 404 handler), `Queryuilder.ts` (`prefer-const`), `interface/index.d.ts` (unused `Request` import), `globalErrorHandler.ts` (unused `next`), and a `no-console` warning in `openRouterClient.ts`. None were introduced by this change; fixing them wasn't part of spec 01's scope and wasn't done, per the user's explicit "don't go beyond spec 01" instruction.
- Wrote 8 new implementation-plan specs (`02` through `09`), one per module, covering the real business logic for the whole backend — per the user's explicit request to review each module's plan before any code is written. Docs only, no source files touched. Ground-truthed against the actual current stub code (not just the older plan doc) via direct file reads, surfacing a few real bugs/gaps along the way (see Known Gaps below).
- Implemented [`02-auth-hardening.md`](specs/02-auth-hardening.md): fixed password re-hash guard, added `TJwtPayload` type with `userRole` for forward-compat admin support, tightened `Request.user` type, centralized JWT expiry config, stripped password from register/login/me responses, renamed `crateUser` → `createUser`, added `GET /auth/me`. All verification steps pass: `yarn build` clean, `yarn lint` clean for touched files, endpoints verified (no password in responses, token includes `userRole: "user"`, re-hash guard functional).
- Implemented [`03-bike-crud.md`](specs/03-bike-crud.md): created `bike.utils.ts` with shared `findOwnedBikeOrThrow`/`bumpOdometerIfHigher` helpers; implemented all 5 bike service functions (create, list, get by id, update, soft delete) with ownership scoping via `req.user.userId`; updated controller with real handlers using `sendResponse`; exported `bumpOdometerIfHigher` for reuse by `fuelLog`/`maintenanceLog` (specs 04/08). `yarn build`/`lint` clean.
- Implemented [`04-fuel-log-and-mileage-closure.md`](specs/04-fuel-log-and-mileage-closure.md): made `totalCost` optional in validation (server computes `litersAdded * pricePerLiter`); `createFuelLogIntoDB` runs ownership check via `findOwnedBikeOrThrow`, computes `totalCost`, saves `FuelLog`, bumps bike odometer via `bumpOdometerIfHigher`, runs full-tank closure (finds previous full-tank log or uses bike creation, sums liters in period, creates `MileageRecord`); `getFuelLogsFromDB` uses `QueryBuilder` with `-date` default sort + pagination; `updateFuelLogInDB`/`deleteFuelLogFromDB` return 409 if referenced by `MileageRecord`. `yarn build`/`lint` clean.
- Implemented [`05-mileage-stats.md`](specs/05-mileage-stats.md): implemented 4 read-only endpoints (`getMileageRecords`, `getMonthlyMileage`, `getYearlyMileage`, `getLifetimeMileage`) with shared `computeMileageForRange` helper; exact `MileageRecord` history + live rolling-average fallback (trailing 10 fuel logs); monthly/yearly/lifetime totals bucketed by `FuelLog.date` (not `createdAt`); inline validation of `targetMonth`/`targetYear` query params returns 400 on bad input; no average fields returned (client computes). `yarn build`/`lint` clean.
- Implemented [`06-maintenance-type-catalog.md`](specs/06-maintenance-type-catalog.md): implemented `createMaintenanceTypeIntoDB` with duplicate-key handling (catches Mongo code 11000 → 409 `AppError`), `getMaintenanceTypesFromDB`; created `seedMaintenanceTypes.ts` + `yarn seed:maintenance-types` — idempotent upsert-by-name for 8 plan types (Engine Oil, Chain Lube 500km, Tire Change, Brake Pads, General Service 3000km, Insurance 365d, Registration/Tax 365d, Other). `yarn build`/`lint` clean.

## Known Gaps / Open Questions

- No automated test suite (`yarn test` is a stub) — inherited from the boilerplate, not yet decided whether to add one.
- 5 pre-existing lint errors + 1 warning remain repo-wide (see Recent Activity above) — none in Bike Log modules, all predate this change, none fixed since it was out of spec 01's scope. Worth a dedicated cleanup pass before this goes live.
- Exact "upcoming" reminder buffer was locked to 50km in spec 08's implementation (the plan doc's example value, explicitly confirmed in the spec).
- `engine-oil-types` and `maintenance-types` endpoints currently have no seed data — [`06-maintenance-type-catalog.md`](specs/06-maintenance-type-catalog.md) and [`07-engine-oil-type-catalog.md`](specs/07-engine-oil-type-catalog.md) both propose seed scripts with specific values, but those values (especially oil-change intervals) are flagged as assumptions needing confirmation, not researched numbers.
- `spending`'s response interface (`TSpendingSummary`) only had a `period` field with no way to select which month/year — [`09-spending-summary.md`](specs/09-spending-summary.md) resolved this by adding `targetMonth`/`targetYear` query params (implemented).

## Next Up

All 8 original Bike Log modules (specs 02–09), the spec-10 `bikeIssue` addition, and the spec-11 `bikeAccessory` addition are implemented. No remaining MVP or post-MVP-scope-expansion backend work. Next steps would be frontend integration (including `bikeIssue` and `bikeAccessory` domain hooks/UI, neither yet built), manual endpoint verification of `bikeAccessory` against a running server, pre-existing lint cleanup, or further Phase-2 features.
