# Progress Tracker

Update this file after every meaningful implementation change.

## Current Phase

All 8 original Bike Log modules (`user`, `bike`, `fuelLog`, `mileageRecord`, `maintenanceType`, `engineOilType`, `maintenanceLog`, `spending`) have real business-logic implementations across specs 02–09. No remaining stubs. A 9th module, `bikeIssue`, was added per spec 10 as a post-MVP scope expansion (issue tracking with an open/resolved lifecycle, simplified in a later pass — see Recent Activity) — complete. A 10th module, `bikeAccessory`, was added per spec 11 as a further post-MVP scope expansion (per-bike purchase wishlist with `urgency`/`status`, plain CRUD, no state machine) — complete. Spec 15 (v2 scope) added rolling-window trend endpoints to `spending`/`mileageRecord` for chart-consuming clients — complete. Spec 16 (v2 scope) added a new `ai` module (cached spending insight, cached mileage insight, grounded bike chat) reusing the already-existing but previously-unused `openRouterClient.ts` plumbing — complete. Spec 17 (v2 scope) added Cloudinary-backed image upload linked to existing schemas (`fuelLog.receiptImage`, `maintenanceLog.serviceImage`, `bikeAccessory.productImage`, `bikeIssue.images[]`), replacing the uncommitted `SendImageCloudinary.ts` scaffolding with a corrected `util/cloudinary.ts` + `middleware/upload.ts` pair — complete. Spec 18 (v2 scope) added a new `bikeManual` module (one owner-manual PDF per bike, chunked and indexed via plain in-process keyword/TF-IDF-style scoring — no embeddings/vector DB) and wired its retrieval into `ai.service.ts`'s `getBikeChatReply` so chat answers can be grounded in manual content (e.g. service intervals) that fuel/maintenance/spending data alone can't cover — complete. Spec 19 (v2 scope) added a new `bikeDocument` module for bike paperwork (registration paper, tax token, driving license, bank receipt, etc.) with an optional expiry date and multiple mixed image/PDF files per record — complete, backend-only for now (no frontend counterpart built in this pass; both clients' own spec docs already exist under their respective `context/specs/`/`ai context/specs/` folders for a future pass).

## Spec Implementation Status

Tracks work items defined in `context/specs/`. Update the moment implementation starts or finishes on a spec.

| Spec                                                                                             | Status      |
| ------------------------------------------------------------------------------------------------ | ----------- |
| [`00-build-plan.md`](specs/00-build-plan.md)                                                     | Not Started |
| [`01-module-scaffolding-and-models.md`](specs/01-module-scaffolding-and-models.md)               | Complete    |
| [`02-auth-hardening.md`](specs/02-auth-hardening.md)                                             | Complete    |
| [`03-bike-crud.md`](specs/03-bike-crud.md)                                                       | Complete    |
| [`04-fuel-log-and-mileage-closure.md`](specs/04-fuel-log-and-mileage-closure.md)                 | Complete    |
| [`05-mileage-stats.md`](specs/05-mileage-stats.md)                                               | Complete    |
| [`06-maintenance-type-catalog.md`](specs/06-maintenance-type-catalog.md)                         | Complete    |
| [`07-engine-oil-type-catalog.md`](specs/07-engine-oil-type-catalog.md)                           | Complete    |
| [`08-maintenance-log-and-reminders.md`](specs/08-maintenance-log-and-reminders.md)               | Complete    |
| [`09-spending-summary.md`](specs/09-spending-summary.md)                                         | Complete    |
| [`10-bike-issue.md`](specs/10-bike-issue.md)                                                     | Complete    |
| [`11-bike-accessory.md`](specs/11-bike-accessory.md)                                             | Complete    |
| [`12-bike-issue-list-sort-order.md`](specs/12-bike-issue-list-sort-order.md)                     | Complete    |
| [`13-bike-accessory-list-sort-order.md`](specs/13-bike-accessory-list-sort-order.md)             | Complete    |
| [`14-bike-accessory-price.md`](specs/14-bike-accessory-price.md)                                 | Complete    |
| [`15-spending-and-mileage-trend-endpoints.md`](specs/15-spending-and-mileage-trend-endpoints.md) | Complete    |
| [`16-ai-integration.md`](specs/16-ai-integration.md)                                             | Complete    |
| [`17-image-file-upload.md`](specs/17-image-file-upload.md)                                       | Complete    |
| [`18-bike-manual-ai-integration.md`](specs/18-bike-manual-ai-integration.md)                     | Complete    |
| [`19-bike-documents.md`](specs/19-bike-documents.md)                                             | Complete    |
| [`20-decimal-number-field-audit.md`](specs/20-decimal-number-field-audit.md)                     | Complete    |
| [`21-weekly-bike-summary-notification.md`](specs/21-weekly-bike-summary-notification.md)         | Complete    |
| [`22-trend-endpoint-6-month-window.md`](specs/22-trend-endpoint-6-month-window.md)               | Complete    |
| [`23-spending-details-export-endpoint.md`](specs/23-spending-details-export-endpoint.md)         | Complete    |
| [`24-error-log-system.md`](specs/24-error-log-system.md)                                         | Complete    |

## Completed

- **Spec 01 — module scaffolding**: `transaction` module removed (was leftover `expense-tracker-server` boilerplate, unrelated to Bike Log). `user` module adapted in place — added `expoPushToken?: string | null` to `TUser`/`user.model.ts`; `argon2` hashing and existing fields left untouched. Seven new modules created under `src/app/modules/`, each with fully implemented model + interface (`spending` has no model, per plan §4), real Zod validation schemas, and stub controller/service functions (controllers throw `AppError(httpStatus.NOT_IMPLEMENTED, ...)`; services are TODO passthroughs). Every planned endpoint from `bike-log-plan.md` §5 is wired into `router/index.ts`'s `routeArray` and reachable. `maintenanceLog.route.ts` exports two routers (`maintenanceLogRouter` for CRUD, `reminderRouter` for `/reminders`) since one module owns two path prefixes.
- Shared infra as cloned: `authCheck`, `validateRequest`, `globalErrorHandler`, `AppError`, `catchAsync`, `sendResponse`, `Queryuilder` — reused as-is for all new modules without changes (see `context/architecture.md`).
- **Spec 02 — auth hardening**: Fixed unconditional password re-hash in `user.model.ts` (added `isModified("password")` guard). Added `TJwtPayload` type with `userId`, `userEmail`, `userRole` to `user.interface.ts`. Tightened `Request.user` type in `src/app/interface/index.d.ts` to `TJwtPayload`. Added `jwt_expires_in` config (from `JWT_EXPIRES_IN`, default `"10d"`) to `src/app/config/index.ts`. In `user.services.ts`: added `.select("-password")` to `createUser` and `loginFromDb` responses; typed `jwtPayload` as `TJwtPayload` with `userRole`; used `config.jwt_expires_in`; added `getMeFromDb(userId)` for new endpoint. In `user.controller.ts`: renamed `crateUser` → `createUser`; added `getMe` controller calling `getMeFromDb(req.user.userId)`. In `user.route.ts`: fixed typo to `createUser`; added `GET /auth/me` with `authCheck`. Verified: `yarn build` clean, `yarn lint` clean for all modified modules, three auth endpoints manually verified (register, login, me) — no password in responses, token includes `userRole: "user"`, re-hash guard works.
- **Spec 03 — bike CRUD**: Created `bike.utils.ts` with shared `findOwnedBikeOrThrow(bikeId, userId)` (returns 404 for non-existent, other-user, or soft-deleted bikes) and `bumpOdometerIfHigher(bike, newReading)` (only increases odometer, never decreases). Implemented all 5 service functions in `bike.service.ts`: `createBikeIntoDB` (forces `owner = userId`, defaults `currentOdometer: 0`), `getBikesFromDB` (filters by `owner` + `isDeleted: false`), `getBikeByIdFromDB` (uses ownership helper), `updateBikeInDB` (ownership check, strips `owner`/`currentOdometer` from payload, uses `Object.assign + save()` for hooks), `deleteBikeFromDB` (soft delete via `isDeleted = true + save()`). Updated `bike.controller.ts` with real `catchAsync` handlers using `sendResponse`. Exported `bumpOdometerIfHigher` from services for reuse by `fuelLog`/`maintenanceLog` (specs 04/08). Verified: `yarn build` clean, `yarn lint` clean for bike module.
- **Spec 04 — fuel log + mileage closure**: Updated `createFuelLogSchema` to make `totalCost` optional (server computes `litersAdded * pricePerLiter`). Implemented `createFuelLogIntoDB` with ownership check via `findOwnedBikeOrThrow`, computes `totalCost`, saves `FuelLog`, bumps bike odometer via `bumpOdometerIfHigher`, runs full-tank closure: finds previous full-tank log (or uses bike creation as period start), sums liters in period, creates `MileageRecord` with `distanceKm`, `litersConsumed`, `mileageKmPerLiter`, `periodStartDate`/`EndDate`, `fuelLogIds`. `getFuelLogsFromDB` uses `QueryBuilder` with `-date` default sort + pagination. `updateFuelLogInDB`/`deleteFuelLogFromDB` return 409 if fuel log referenced by any `MileageRecord`. Controller wraps all endpoints with `sendResponse`. Verified: `yarn build` clean, `yarn lint` clean for fuelLog module.
- **Spec 05 — mileage stats**: Implemented 4 read-only endpoints in `mileageRecord.service.ts` + `mileageRecord.controller.ts`. Shared internal `computeMileageForRange` helper avoids duplicate logic. `getMileageRecordsFromDB`: exact `MileageRecord` history sorted by `periodEndDate` + live rolling-average fallback (trailing 10 `FuelLog`s, `distance = last.odometerReading - first.odometerReading`, `liters = sum(litersAdded)`, `approxMileage = distance / liters`; returns `null` if <2 logs). `getMonthlyMileageFromDB`: date range `YYYY-MM`, `FuelLogModel.find({ bike, date: {$gte, $lte} })` bucketed by `date` (not `createdAt`), computes `totalDistanceKm = lastOdometerInMonth - firstOdometerBeforeOrInMonth`, `totalLitersConsumed`, `fuelLogCount` (no average — client divides). `getYearlyMileageFromDB`: loops 12 months of target year, same per-month computation. `getLifetimeMileageFromDB`: all `FuelLog`s from earliest to latest, `totalDistanceKm = last.odometerReading - first.odometerReading`, `totalLitersConsumed`, `fuelLogCount`. Inline validation of `targetMonth`/`targetYear` query params returns 400 on malformed input. Verified: `yarn build` clean, `yarn lint` clean for mileageRecord module.
- **Spec 06 — maintenance type catalog**: Implemented `createMaintenanceTypeIntoDB` with duplicate-key handling (catches Mongo code 11000, returns 409 `AppError` with clear message), `getMaintenanceTypesFromDB` (plain `.find()` sorted by name). Controller wraps with `sendResponse`. Created `src/scripts/seedMaintenanceTypes.ts` + `yarn seed:maintenance-types` script — idempotent upsert-by-name for 8 plan-defined types: Engine Oil (interval driven by `EngineOilType`), Chain Lube (500km), Tire Change, Brake Pads, General Service (3000km), Insurance (365 days), Registration/Tax (365 days), Other. Verified: `yarn build` clean, `yarn lint` clean for maintenanceType module.

- **Spec 09 — spending summary**: Implemented `getSpendingSummaryFromDB` in `spending.service.ts` — ownership check; date-range resolution (month/year/lifetime with the same inline validation pattern as spec 05); parallel `FuelLog.find` + `MaintenanceLog.find({ bike }).populate("maintenanceType", "name")`; JS `reduce`-based aggregation (fuel total + maintenance grouped by `maintenanceType.name`); `categoryBreakdown` sorted descending by total; response includes `period`, optional `targetMonth`/`targetYear`, `totalSpending`, `categoryBreakdown`. Extended `TSpendingSummary` type in `spending.interface.ts` to add `"lifetime"` to the `period` union and optional `targetMonth`/`targetYear`. Wired controller in `spending.controller.ts` with inline validation (`period` must be month/year/lifetime; `targetMonth` required for month; `targetYear` required for year; malformed inputs → 400). `yarn build` clean, `yarn lint` clean (no new errors).

- **Spec 08 — maintenance log + reminders**: Implemented all 6 service functions in `maintenanceLog.service.ts` — `createMaintenanceLogIntoDB` (ownership + referential checks on `maintenanceType`/`oilType`, server-computed `nextDueOdometer`, odometer bump), `getMaintenanceLogsFromDB` (QueryBuilder with `-serviceDate` sort + optional `maintenanceType` filter), `getMaintenanceLogByIdFromDB`, `updateMaintenanceLogInDB` (recomputes `nextDueOdometer` if `odometerReading` or `intervalKmUsed` changes, referential checks on update, strips client-supplied `nextDueOdometer`), `deleteMaintenanceLogFromDB` (soft delete), `getRemindersFromDB` (groups by `maintenanceType` → most recent log, km-based status with 50km buffer, date-based status with 14-day buffer, omits entries that are neither due nor upcoming). Wired all 6 controller handlers in `maintenanceLog.controller.ts` with `sendResponse`. No validation/route/model changes needed. `yarn build` clean, `yarn lint` clean (no new errors).

## Recent Activity

- **2026-08-29 — Spec 24 (automatic error logging system, 30-day rolling retention, admin-only read access) implemented, per direct user request, scope limited strictly to spec 24.** Every error reaching `globalErrorHandler` (the app's existing single choke point for `AppError`, `ZodError`, Mongoose `ValidationError`/`CastError`, Mongo duplicate-key, and any unclassified error) is now persisted to a new `ErrorLog` collection **before** the error response is sent (`await`ed, not fire-and-forget — a deliberate correctness call for Vercel serverless, where an un-awaited write issued after the response is flushed isn't guaranteed to complete), wrapped in its own `try/catch` so a logging failure never blocks or replaces the real error response. New `errorLog` module (`src/app/modules/errorLog/`: `.interface.ts`/`.model.ts`/`.service.ts`/`.controller.ts`/`.route.ts`) with a MongoDB TTL index on `createdAt` (`expireAfterSeconds: 2592000` = 30 days) for fully automatic rolling-window deletion — no manual delete endpoint exists or was requested. New `src/app/middleware/adminCheck.ts` — the **first real consumer** of the `userRole` field that's existed on `TUser`/`TJwtPayload` since spec 02 as forward-compatible scaffolding but was never read anywhere until now; 403s any request whose `req.user.userRole !== "admin"`. Mounted `GET /admin/error-logs` (paginated list, `Queryuilder`-based, newest-first default, `status`/`method` exact-match filters) and `GET /admin/error-logs/:id` (full detail incl. `stack`) behind `authCheck` + `adminCheck` at a new `/admin` route-namespace prefix — the first admin-only resource in this codebase. No admin-promotion endpoint was built (explicit scope boundary, per the spec) — the first admin account still requires a direct DB write (`userModel.updateOne({email}, {$set:{userRole:"admin"}})`), and since `userRole` is baked into the JWT at login time, a promoted account must log in again before its token reflects the new role. Added a new "Error Logs" folder (List, Get by ID) and `errorLogId` variable to `postman/bikelog-api.postman_collection.json`, and a matching "Error Logs" section to `postman/dummy-data.md`.

  Verified: `yarn build` clean; `yarn lint` — 0 errors, same pre-existing 14 warnings, none new (the new `console.error` in `globalErrorHandler.ts`'s catch block carries its own `eslint-disable-next-line no-console`, matching the repo's existing tolerated pattern). Extensively exercised live against a temporary local server instance (isolated port 5099, real dev DB) with two throwaway registered users (one manually promoted to `admin` via a temporary script, then re-logged-in for a fresh JWT — confirmed `userRole: "admin"` in the decoded token): a deliberately-malformed `POST /bikes` (400, Zod), a nonexistent-bike-id `GET` (404, `AppError`), and a bad-login attempt (404, unauthenticated) each produced exactly one correct `ErrorLog` entry, with `userId`/`userEmail` populated for the authenticated cases and `null` for the unauthenticated one; `GET /admin/error-logs` 403'd with a non-admin token and 401'd with no token (both of which themselves became new log entries, confirming the admin-gate rejections flow through the same handler as everything else); with a real admin token it returned the paginated `{result, meta}` list newest-first, `?status=400` correctly narrowed to the 1 matching entry out of 5; `GET /admin/error-logs/:id` returned the full document including a non-empty `stack`, and a nonexistent id correctly 404'd. Confirmed the TTL index actually exists on the live collection via `errorLogModel.syncIndexes()` + `.collection.indexes()`: `{createdAt: 1}` with `expireAfterSeconds: 2592000`, plus the compound `{status: 1, createdAt: -1}` index — real 30-day expiry timing itself wasn't (and can't practically be) live-verified in one sitting, so the mechanism was verified instead, per the spec's own note. The award-before-response ordering and try/catch swallowing in `globalErrorHandler.ts` were verified by code reading rather than by severing the DB connection mid-request, since doing so would have disrupted the same shared dev database the rest of this pass was actively using. All fixtures (2 users, 5 test-generated error-log entries) were cleaned up afterward via temporary in-tree scripts reusing the app's own `config`/`mongoose.connect` (deleted immediately after running, same pattern as every prior spec's cleanup pass). Spec 24's own Status/Implementation/Verify sections updated in place to reflect exactly what was built and verified.

- **2026-08-17 — Spec 23 (spending details export endpoint) implemented, per direct user request, scope limited strictly to spec 23.** New `GET /bikes/:bikeId/spending-summary/details?period=month|year|lifetime&targetMonth=YYYY-MM&targetYear=YYYY` — returns the individual fuel-log/maintenance-log line items behind a period's totals (the raw detail `/spending-summary` discards after reducing into `categoryBreakdown`), for the web client's upcoming PDF export feature (`bikelog_client-web-` spec 21) to consume; no PDF/file generation on this backend. Added `TSpendingRecordSource`, `TSpendingRecord` (`date`, `category`, `description`, `amount`, `vendor: string | null`, `remarks: string | null`, `source: "fuel" | "maintenance"`), and `TSpendingDetails` to `spending.interface.ts`. In `spending.service.ts`: extracted the inline month/year boundary-date math that used to live only inside `getSpendingSummaryFromDB` into a new shared `resolveSpendingDateRange(period, targetMonth?, targetYear?)`, now used by both `getSpendingSummaryFromDB` and the new `getSpendingDetailsFromDB`; extended `computeSpendingForRange` itself to also build and return a chronologically-sorted `records: TSpendingRecord[]` alongside its existing `totalSpending`/`categoryBreakdown` (fuel records get a synthesized `"${litersAdded}L (Full Tank) @ ৳${pricePerLiter}/L"`-style description, maintenance records use `partsReplaced.join(", ")` or fall back to the maintenance type's name) — rather than adding a separate `computeSpendingDetailsForRange` sibling, per the spec's own "whichever avoids duplicating the fetch/reduce logic" call. This keeps the fetch/reduce logic in exactly one place; `getSpendingTrendFromDB` and `notification.service.ts`'s existing calls to `computeSpendingForRange` are unaffected (both destructure only `totalSpending`/`categoryBreakdown`, ignoring the new field). Added `getSpendingDetails` to `spending.controller.ts` (identical inline `period`/`targetMonth`/`targetYear` validation to `getSpendingSummary`, no new validation contract invented) and `GET /details` to `spending.route.ts` behind `authCheck`. Added a "Spending Details" request to the Postman collection's existing "Spending Summary" folder, matching the other 4 requests' style. Verified: `yarn build` clean; `yarn lint` shows only the same pre-existing 14 warnings / 0 errors baseline, none new. Extensively exercised live against a temporary local server instance (real dev DB) with a throwaway user, one bike, a maintenance-type catalog entry, 3 fuel logs (one dated outside the target month, to confirm exclusion), and 2 maintenance logs (one soft-deleted afterward, to confirm exclusion): `period=month` returned `records` in correct ascending-date order with the out-of-month and soft-deleted logs both correctly absent; `totalSpending`/`categoryBreakdown` matched `/spending-summary`'s own response byte-for-byte for `month`, `year`, and `lifetime` alike (no drift between the two endpoints); a fuel log created without `fuelStation`/`notes` came back with literal `vendor: null`/`remarks: null` (not `undefined` or `""`); missing/invalid `period`, `period=month` without `targetMonth`, and `period=year` without `targetYear` all correctly `400`'d; a second throwaway user got `404` requesting the first user's bike's details, confirming ownership enforcement holds on the new route same as the existing one. All fixtures (2 users, 1 bike, 1 maintenance type, 3 fuel logs, 2 maintenance logs) were cleaned up afterward — the bike soft-deleted through the app's own `DELETE /bikes/:id` endpoint first, both users hard-deleted via a temporary in-tree script (`src/scripts/__tmpSpec23Cleanup.ts`, reusing the app's own `config`/`mongoose.connect`, deleted immediately after running — same pattern as every prior spec's cleanup pass). Spec 23's own Status/Implementation/Verify sections updated in place to reflect exactly what was built and verified. This closes out the last outstanding backend spec — no remaining MVP or post-MVP-scope-expansion backend work as of this entry.

- **2026-08-17 — Spec 22 (trend endpoints, 6-month window) implemented, per direct user request, scope limited strictly to spec 22.** This was a re-verification spec, not a code-change spec — per the spec's own Design section, `6` was already inside the existing validated `1–24` range on both `GET .../spending-summary/trend` and `GET .../mileage/trend` (spec 15), and the rolling-window loop (`for (let i = months - 1; i >= 0; i--)`) has no `months === 3`-specific assumption, so **no service/controller/route code was changed** — confirmed by re-reading `spending.service.ts:122-145`/`spending.controller.ts` and `mileageRecord.service.ts:219-236`/`mileageRecord.controller.ts` directly. `ai.service.ts:105`'s unrelated hardcoded `getMileageTrendFromDB(bikeId, 3)` call (AI-context feature, not a client-facing chart) was deliberately left untouched, per the spec's own explicit scope note. Only change made: updated the "Spending Trend" and "Mileage Trend" example requests in `postman/bikelog-api.postman_collection.json` from `months=3` to `months=6` (both the `raw` URL and the `query` array) to match the web client's new default usage — cosmetic, no effect on endpoint behavior. Verified: `yarn build` clean; `yarn lint` shows only the same pre-existing 14 warnings / 0 errors baseline; `git status` confirms zero `src/` diff. Extensively exercised live against a temporary local server instance (isolated port 5099, real dev DB) with a throwaway registered user, one bike, and fuel logs dated Feb–Aug 2026 (Feb planted deliberately just outside the 6-month window): `GET .../mileage/trend?months=6` and `GET .../spending-summary/trend?months=6` both returned exactly 6 `monthlySummary` entries (`2026-03` through `2026-08`), with the Feb 2026 log correctly excluded from every entry's totals on both endpoints; omitting `months` on both endpoints still defaulted to `3` (3 entries, no regression); `months=25` and `months=0` both still 400'd on both endpoints. The current date (2026-08-17) doesn't put a 6-month window across a calendar-year boundary, so that specific case wasn't re-exercised live here — the underlying `Date`-based rolling-window math is unchanged from spec 15, where it was already verified across a year boundary at `N=14`. All fixtures (1 bike, 7 fuel logs, 1 throwaway user) were cleaned up afterward — the bike soft-deleted through the app's own `DELETE /bikes/:id` endpoint first, the user hard-deleted via a temporary in-tree script (`src/scripts/__tmpSpec22Cleanup.ts`, reusing the app's own `config`/`mongoose.connect`, deleted immediately after running — same pattern as every prior spec's cleanup pass). Spec 22's own Status/Implementation/Verify sections updated in place to reflect exactly what was checked and how.

- **2026-08-06 — Spec 21 amended: send time changed from Friday 08:00 to Thursday 10pm (22:00) Asia/Dhaka, per direct user follow-up instruction.** Not just a cron-expression tweak — 22:00 Thursday falls _inside_ that same week (which doesn't end until Thursday midnight), so `notification.utils.ts`'s week-boundary function had to change semantics too, not just its call time. Renamed `getLastCompletedWeekRange` → `getCurrentWeekRange` and rewrote it to return "the Friday–Thursday window containing `now`" instead of "the last fully completed window before `now`" — at trigger time this now correctly resolves to the week that's about to finish that same night (today's date, end-of-day, not yet reached), rather than stepping back an extra week the way the old "completed" framing would have. Documented one accepted, deliberate gap this introduces: a fuel log dated in the final 22:00–23:59:59.999 window of a Thursday is never captured by any week's digest (this week's already went out; next week's window starts fresh from the following Friday) — a minor tradeoff for a fixed 10pm send time, not a bug, called out explicitly in the function's own comment and in the spec doc. Updated `notification.service.ts`'s import/call site and inline comment to match. Updated `.github/workflows/weekly-summary-cron.yml`'s schedule from `0 2 * * 5` (Friday 08:00 Dhaka) to `0 16 * * 4` (Thursday 22:00 Dhaka = 16:00 UTC), plus its header comment. Updated the Postman collection's "Trigger Weekly Summary" request description and `postman/dummy-data.md`'s Cron section to describe the new "current week, sent 10pm Thursday" behavior instead of the old "last completed week" framing. `yarn build` clean; `npx eslint src/app/modules/notification` clean (0 output). Manually re-verified the new `getCurrentWeekRange` against 3 real dates via a standalone Node script mirroring its exact logic: the actual Thursday-22:00-Dhaka trigger case (correctly resolves to today's own not-yet-ended week), a case where the resulting week spans a Feb→Mar month boundary, and a mid-week manual-trigger case (a Tuesday) confirming the function correctly returns the in-progress current week (with `endDate` legitimately in the future relative to `now`, expected for a manual test). Did not re-run the full live end-to-end Postman/temporary-server pass this time (already covered thoroughly in the original implementation entry below, using the same underlying send pipeline/skip logic — only the week-boundary calculation and its call time actually changed). Spec 21's own Status/Goal/Design/Implementation/Verify sections updated in place to describe the current, shipped Thursday-10pm behavior rather than the original Friday-morning design.

- **2026-08-06 — Spec 21 (weekly bike summary push notification, free stack) implemented, per direct user request, scope limited strictly to spec 21.** Extended the `user` module with `POST /auth/push-token` (`authCheck`-protected, `pushTokenSchema`, `updatePushToken`) — the endpoint bike-log-plan.md's §7.2 anticipated but never built, at its real mount point (`/auth/push-token`, not the old plan doc's stale `/users/push-token`). Exported the two previously-private range helpers `computeMileageForRange` (`mileageRecord.service.ts`) and `computeSpendingForRange` (`spending.service.ts`) from their modules' service objects — purely additive, no behavior change to any existing call site — so the new module could reuse them instead of duplicating range-query logic. New `notification` module (`src/app/modules/notification/`): `notification.utils.ts`'s `getLastCompletedWeekRange` computes the most recently completed **Friday–Thursday** week (not the calendar week, per direct user instruction) against a fixed `WEEK_TZ_OFFSET_MINUTES = 360` (Asia/Dhaka, UTC+6, no DST) constant — flagged as an assumption based on the `৳` currency already used throughout this app's UI, not confirmed with the user directly; `notification.service.ts`'s `sendWeeklySummaries` queries every user with a non-null `expoPushToken`, their bikes, computes mileage+spending per bike for that week via the two newly-exported helpers, skips bikes with zero fuel logs that week (no blank digest), and batch-sends via `expo-server-sdk` (`Expo.chunkPushNotifications`/`sendPushNotificationsAsync`); `notification.controller.ts`'s `triggerWeeklySummary` checks an `x-cron-secret` header against a new `config.cronSecret` instead of `authCheck` (machine-to-machine, not a logged-in user); mounted at `POST /cron/weekly-summary`. Added `expo-server-sdk@7.0.0` (`yarn add`, free/MIT). Added `cronSecret: process.env.CRON_SECRET` to `config/index.ts` — **did not touch the real `.env` file** (deliberately not reading/writing a secrets file); the user needs to add `CRON_SECRET` there and to the deployed Vercel project's env vars themselves. New `.github/workflows/weekly-summary-cron.yml` (Option A from the spec — GitHub Actions `schedule` cron, `0 2 * * 5` = Friday 08:00 Dhaka, plus a `workflow_dispatch` manual trigger) — needs 2 repository secrets (`API_BASE_URL`, `CRON_SECRET`) added on GitHub's side before it can actually fire; Option B (Vercel Cron) was not built, per the spec's own recommendation. Added "Update Push Token" (Auth folder) and a new "Cron" folder ("Trigger Weekly Summary") to `postman/bikelog-api.postman_collection.json` (plus a new `cronSecret` collection variable), and matching sections to `postman/dummy-data.md` (including correcting the "fields never accepted" table's `expoPushToken` row, which is now accepted, just only via this one dedicated endpoint).

  Verified: `yarn build` clean; `yarn lint` / `npx eslint` scoped to every touched/new module both clean (0 errors, same 14 pre-existing `no-console` warnings, none new). Manually verified `getLastCompletedWeekRange` against 3 real dates via a standalone Node script mirroring its exact logic — a Friday-cron-trigger case, a case where the completed week itself spans a Feb→Mar month boundary, and a case spanning the Dec 2025→Jan 2026 year boundary — all three correct. **Extensively exercised live against a temporary local server instance** (isolated port 5099, real dev DB): registered a throwaway user, created 2 bikes (one with a fuel log dated inside the live-computed target week, one with none), registered a syntactically-valid-but-fake Expo push token via the new endpoint, then called `POST /cron/weekly-summary` — confirmed a wrong/missing secret both 401 with nothing sent; with the correct secret, the bike with a fuel log was correctly _not_ skipped and a real push request reached Expo's actual push API (confirmed by the response: `notificationsFailed: 1`, Expo's own service correctly rejecting the fake token — proof the full send pipeline ran for real, not just that a message object was built), while the empty bike was correctly skipped (`bikesSkipped: 1` on a repeat call with both bikes present); also confirmed empty-token 400s via Zod. All fixtures (2 bikes, 1 fuel log, 1 mileage record, 1 throwaway user) were cleaned up afterward — bikes soft-deleted through the app's own `DELETE /bikes/:id` endpoint first, the user hard-deleted via a temporary in-tree script (`src/scripts/__tmpCronCleanup.ts`, reusing the app's own `config`/`mongoose.connect`, deleted immediately after running — same pattern as every prior spec's cleanup pass). **Not verified**: actual on-device push delivery (needs a real device + the app-side spec 24, which is code-complete but not yet run on a real device in this environment) and the `.env`/Vercel/GitHub-secrets steps that are outside this repo's own scope. Spec 21's own Status/Implementation/Verify sections updated to reflect exactly what was built and verified.

- **2026-08-06 — Spec 20 (decimal number field audit) executed, per direct user request, scope limited strictly to spec 20.** User reported that entering a decimal odometer reading (e.g. `408.8`) on the frontend triggers a "whole number required" toast, and asked for a cross-project analysis before any fix. This spec is the backend half of that analysis — re-confirmed, by re-reading `fuelLog.validation.ts`, `maintenanceLog.validation.ts`, `bike.validation.ts` (none use `.int()`), all four Mongoose models (`fuelLog`/`maintenanceLog`/`bike`/`mileageRecord`, all plain `{type: Number}`), and every service file for `Math.round`/`Math.floor`/`Math.ceil`/`parseInt`/`Number.isInteger` (only unrelated hit: `maintenanceLog.service.ts`'s `daysRemaining` reminder-buffer computation), that the backend already fully supports decimal odometer/interval values end to end and was never the source of the reported bug. **No backend code was changed** — this spec's own Implementation section calls for none. The two optional hardening items it flags (a server-side 2-decimal precision clamp, a `dummy-data.md` doc note) were not implemented, since neither was requested and both are explicitly optional judgment calls in the spec. The actual bug's root causes live in the two frontend repos' own sibling specs: `bikelog_client-web-/context/specs/19-decimal-number-field-fix.md` (missing HTML `step` attribute defaulting `<input type="number">` to whole-number-only native validation) and `bikelog_app/ai context/specs/23-decimal-number-field-fix.md` (explicit `INT_REGEX`+`parseInt` gate in two form modals) — both implemented in the same session as this spec. Spec 20's own Status/Implementation/Verify sections updated to `✅ Complete`.

- **2026-07-29 — Spec 19 (bike documents/papers: registration paper, tax token, driving license, bank receipt, etc., with optional expiry date and multiple mixed image/PDF files) implemented, per direct user request, scope limited strictly to spec 19 (backend only — no client work).** New `bikeDocument` module at `/bikes/:bikeId/documents`, following the exact design in `context/specs/19-bike-documents.md` and mirroring `bikeIssue`'s file layout/ownership pattern. Schema: `bike` ref, `title` (required, deliberately free-text — no category enum, since the user described the feature as "type of paper or title" rather than a fixed list), optional `description`/`expiryDate`, `files[]` (array of subdocuments, each keeping its own auto `_id` for individual deletion). Two-step create-then-attach, same UX as `bikeIssue`: `POST /` is plain JSON (no file field), files are attached afterward via `POST /:id/files` (up to 10 per request, additive) / `DELETE /:id/files/:fileId`.

  New shared upload plumbing for the mixed image+PDF requirement (neither existing multer instance fit): `src/app/middleware/uploadDocument.ts` — a third `multer.memoryStorage()` instance (distinct from `upload.ts`'s image-only `CloudinaryStorage` and `uploadManual.ts`'s PDF-only one), with a `fileFilter` accepting `image/*` OR `application/pdf`, 20MB/file limit. `src/app/util/cloudinary.ts` gained `uploadDocumentBuffer(buffer, originalName, mimetype)` — picks Cloudinary `resource_type` (`"image"` vs `"raw"`) per file based on its own mimetype (unlike the existing `uploadRawBuffer`, which hardcodes `"raw"` for `bikeManual`'s PDF-only case) and returns the chosen `resourceType` so it can be persisted per-file for correct-typed deletes later. `src/app/interface/image.interface.ts` gained `TCloudinaryFile` (superset of `TCloudinaryImage` adding `resourceType`/`originalName`/`mimeType`), reused as-is, `TCloudinaryImage` itself untouched.

  List ordering (`GET /`) defaults to soonest-expiry-first, with no-expiry documents sorting last — deliberately **not** a single Mongo `.sort("expiryDate")` call, since ascending sort treats a missing field as less-than-any-value (i.e. first, not last), and this codebase's house style avoids aggregation pipelines for this kind of compound derived ordering (same reasoning `bikeAccessory`'s spec-13 list endpoint already established). Instead runs two plain `find()`s — documents with an `expiryDate` sorted ascending, documents without sorted by `-createdAt` — concatenates them in that fixed order, then paginates over the merged array in memory. A client-supplied `?sort=` fully overrides this default and falls back to the ordinary single-query `QueryBuilder` path.

  **Discovered during implementation (real pre-existing gap, not introduced by this spec but first surfaced by its own Verify checklist item): `multer.MulterError` (e.g. `LIMIT_UNEXPECTED_FILE` from exceeding the 10-file-per-request cap) isn't an `AppError` and carries no `.status`, so `globalErrorHandler`'s generic fallback silently returned a raw `500` instead of a clean `400`.** This same gap exists identically in `bikeIssue`'s pre-existing `upload.array("images", 5)` route (a 6th image in one request would 500 there too) — not something this spec's own new middleware introduced, just the first time a spec's own Verify checklist explicitly required the clean-400 behavior enough to catch it. Fixed narrowly: a small route-local wrapper (`handleDocumentUpload` in `bikeDocument.route.ts`) invokes `uploadDocument.array("files", 10)` directly and catches `multer.MulterError` itself, converting it to `AppError(400, ...)` before calling `next()`. Deliberately **not** fixed by patching the shared `globalErrorHandler.ts` — that would silently change behavior for every other existing upload route in the app (`bikeIssue`, `fuelLog`, `maintenanceLog`, `bikeAccessory`, `bikeManual`), which is beyond this spec's own scope; flagging the `bikeIssue` instance of the same gap as a possible follow-up rather than fixing it here.

  Added a full **Bike Documents** folder (Create/List/Get/Update/Delete + Add Files + Delete File, the files routes as `multipart/form-data`) to `postman/bikelog-api.postman_collection.json`, with new collection variables `bikeDocumentId`/`bikeDocumentFileId` **auto-captured** by the Create/Add-Files requests (unlike the pre-existing `bikeAccessoryId`/`bikeIssueId` gap where neither module has a Create/List request to capture from). Added a matching "Bike Documents" section to `postman/dummy-data.md`.

  Verified: `yarn build` clean; `yarn lint` shows only the same pre-existing `no-console` warnings (14 warnings, 0 errors), none new. Extensively exercised live against a temporary local server instance (isolated port 5099) with two throwaway registered users, a real bike, and real generated test files (a 1x1 PNG and a minimal syntactically-valid PDF) uploaded to real Cloudinary (not mocked): created a title-only document (confirmed `expiryDate` absent, not `null`) and three more with past/near/far expiry dates; confirmed the default list ordering was exactly earliest-expiry-first with the no-expiry document last; uploaded one image + one PDF to a single document in one request and confirmed both appear in `files[]` with the correct `resourceType` and distinct `_id`s, both URLs fetchable (`200`); deleted just the image file by its `_id` and confirmed via direct HTTP that only its Cloudinary asset 404'd while the PDF's stayed live; confirmed a `.txt` upload and an 11th-file-in-one-request both correctly 400 (after the `MulterError` fix above — the 11-file case initially 500'd, confirming the bug before the fix and its absence after); `PATCH`ing a document's `description` left its `files[]` completely untouched; soft-deleted the document with a file still attached and confirmed via Cloudinary's own **Admin API** (`cloudinary.api.resource(...)`, not just the delivery URL) that the asset was actually destroyed — the delivery URL itself kept returning a cached `200` from CDN edge-caching the test's own earlier fetches, which is expected `destroy()`-without-`invalidate` behavior, not a bug, and worth remembering the next time a live Cloudinary-delete check is done against a URL that's already been fetched in the same session; confirmed the deleted document no longer appears in `GET` list/detail. A second real throwaway user got `404` attempting to list or create documents against the first user's bike — ownership enforcement holds. All fixtures (2 users, 1 bike, 4 documents, all uploaded test files) were cleaned up afterward through the app's own delete endpoints first (so Cloudinary assets were actually destroyed, not just detached), then the 2 throwaway users hard-deleted directly against the database via a temporary script reusing the app's own `config`/`mongoose.connect` (deleted immediately after running, same pattern as every prior spec's cleanup pass, since no user-delete API endpoint exists).

- **2026-07-29 — Spec 18 (bike owner-manual PDF upload + AI chat grounding) implemented, per direct user request, scope limited strictly to spec 18.** New `bikeManual` module at `/bikes/:bikeId/manual` (`POST` upload-or-replace, `GET` metadata-only, `DELETE`), following the exact design in `context/specs/18-bike-manual-ai-integration.md`. New `src/app/middleware/uploadManual.ts` — a separate `multer.memoryStorage()` instance (PDF-only `fileFilter`, 20MB limit) distinct from `upload.ts`'s existing image-only Cloudinary-streaming instance, since manual text extraction needs the raw buffer locally. `src/app/util/cloudinary.ts` gained `uploadRawBuffer(buffer, originalName)` (wraps `cloudinary.uploader.upload_stream` with `resource_type: "raw"` in a Promise) and `deleteCloudinaryImage` gained an optional `resourceType: "image" | "raw" = "image"` param — backward-compatible, confirmed all 7 pre-existing call sites (`fuelLog`, `maintenanceLog`, `bikeAccessory`, `bikeIssue`) still use the single-arg form untouched.

  `bikeManual.utils.ts` implements the retrieval pipeline exactly per spec: `chunkManualText` slides a 220-word window with 40-word overlap (step 180, capped at 500 chunks/manual); `tokenize` lowercases/strips punctuation/drops stopwords+1-char tokens; `scoreAndRankChunks` scores chunks against the question via a smoothed `(1 + ln(tf)) * idf`-style heuristic (plain JS, no embeddings API, no vector DB — explicit product decision matching this codebase's aggregation-convention house style), keeps score>0 chunks, takes the top-K, then re-sorts that slice by `chunkIndex` ascending so injected excerpts read in original manual order. `bikeManual.service.ts`'s `uploadBikeManualIntoDB` extracts text via `pdf-parse` **before** touching Cloudinary/DB (so a bad/unparseable PDF fails with zero side effects), then on replace deletes the old raw Cloudinary asset + old chunks before inserting the new ones. `Bike` gained an optional, additive-only `manual?: TBikeManualMeta` subdocument (`bike.interface.ts`/`bike.model.ts`, `_id: false`, no `required`/`default` — existing bikes remain valid untouched). `ai.service.ts`'s `getBikeChatReply` now derives `latestUserQuestion` from the last `role: "user"` message, adds a 4th parallel fetch (`bike.manual ? bikeManualServices.getRelevantManualChunksForChat(...) : Promise.resolve([])`, `MANUAL_CHUNK_TOP_K = 4`), and injects a labeled "Relevant excerpts from the owner's manual" section into the system prompt — omitted entirely (not an empty heading) when no manual exists or no chunk scores above zero. `ai.validation.ts` needed no change — the server-builds-the-whole-system-prompt security property is untouched. Added `pdf-parse@^1.1.4` + `@types/pdf-parse@^1.1.5` to `package.json` (the 1.x line deliberately, not the `2.x`/`latest` dist-tag, since `@types/pdf-parse` only matches the 1.x CommonJS API surface).

  Per direct user instruction: **no frontend work** — this is backend-only for now, manual upload is tested via Postman while the user is the sole user of the app pre-launch. Added a new **Bike Manual** folder (`Upload/Replace Bike Manual`, `Get Bike Manual`, `Delete Bike Manual`) to `postman/bikelog-api.postman_collection.json` (reuses the existing `{{bikeId}}` collection variable, no new variable needed) and a new "Bike Manual" section to `postman/dummy-data.md` explaining the upload-or-replace semantics and that chat grounding happens automatically once a manual exists (no separate "search" endpoint to call).

  Verified: `yarn build` clean; `yarn lint` shows only the same 4 pre-existing repo-wide errors + pre-existing warnings (one new `no-console` warning in `cloudinary.ts`, same tolerated pattern as `openRouterClient.ts`), none new. Extensively exercised live against a temporary local server instance (isolated port 5099, not the possibly-running dev instance) with two throwaway registered users and a real generated PDF (a LibreOffice-converted owner's-manual-style text with specific, checkable service intervals — spark plug every 6000km, air filter every 12000km, etc.): `GET` before any upload returned `hasManual: false` with `200` (not `404`); upload returned `chunkCount: 1` and a real Cloudinary raw URL; a chat question about the spark-plug/air-filter interval at 1200km got back a reply citing the _exact_ 3000km/6000km/12000km figures from the PDF, not generic/invented ones; an unrelated question (recommended tire brand for racing) got an honest "the data doesn't cover this" reply instead of a hallucination; re-uploading a second, different PDF (with a revised 9000km spark-plug interval) confirmed via direct HTTP to the _old_ Cloudinary raw URL that it now 404s (old asset actually destroyed, not just overwritten in the DB), and a follow-up chat question correctly reflected the _new_ 9000km figure; `DELETE` removed the Cloudinary asset (verified via direct HTTP 404) and chunks, `GET` reverted to `hasManual: false`, a second `DELETE` on an already-empty manual correctly 404'd, and a follow-up chat question correctly stopped citing manual content; a non-PDF `.txt` upload attempt was rejected with a clean `400` before ever reaching Cloudinary; a second real throwaway user got `404` on `GET`/`POST`/`DELETE` against the first user's bike's manual endpoints — ownership enforcement holds. Also regression-tested one existing spec-17 image endpoint (`PUT`/`DELETE .../fuel-logs/:id/image`) end-to-end to confirm `deleteCloudinaryImage`'s new optional param didn't break the default `"image"` resource-type path. All throwaway fixtures (2 users, 1 bike, 1 fuel log, 1 mileage record, 2 manual uploads) were cleaned up afterward — the two throwaway users were hard-deleted directly against the database via a temporary script reusing the app's own `config`/`mongoose.connect` (deleted immediately after running, same pattern as prior specs' cleanup passes, since no user-delete API endpoint exists), the bike was soft-deleted through the app's own `DELETE /bikes/:id` endpoint first, and both manual uploads/the fuel-log receipt image were removed through their own delete endpoints so their Cloudinary assets were properly destroyed rather than left orphaned.

- **2026-07-27 — Spec 17 (image/file upload via Cloudinary, linked to existing schemas) implemented, scope limited strictly to spec 17.** Deleted the uncommitted, not-yet-wired `src/app/util/SendImageCloudinary.ts` scaffolding and replaced it with two files rather than patching it in place, because the original had three real problems worth not inheriting (same pattern as the reference `lms_server` project it was modeled on): it uploaded every file to Cloudinary **twice** (once automatically via multer's `CloudinaryStorage`, then again via a manual `cloudinary.uploader.upload()` call on the same path — dropped, since `CloudinaryStorage` alone already sets `req.file.path`/`req.file.filename` to the `secure_url`/`public_id`), it swallowed upload errors with `console.log` instead of throwing (fixed to throw `AppError`s that flow through the existing `catchAsync`/`globalErrorHandler` path), and it never persisted `public_id` so a replaced/deleted image could never be cleaned up (fixed by storing `{ url, publicId }` — never a bare string — everywhere). New `src/app/util/cloudinary.ts` (single `cloudinary.config()` call + `deleteCloudinaryImage(publicId)`, a best-effort wrapper that catches/logs rather than throwing, since a failed cleanup shouldn't block the caller's own delete/replace action) and `src/app/middleware/upload.ts` (the single `upload` multer instance every route below uses, backed by `CloudinaryStorage`, with an image-only `fileFilter` throwing a proper `AppError(400, ...)` — not a raw `Error` that would've fallen through to an unhelpful 500 — and a 5MB size limit). New shared `src/app/interface/image.interface.ts` exporting `TCloudinaryImage = { url, publicId }`.

  Added optional, additive-only image fields to four existing schemas (no `required`/`default`, so pre-existing documents stay valid untouched — no migration): `fuelLog.receiptImage?` (single), `maintenanceLog.serviceImage?` (single), `bikeAccessory.productImage?` (single), `bikeIssue.images?` (array — each subdocument keeps its own auto `_id`, used to target one image for deletion out of several). Deliberately **not** added to `bike` itself, per direct product decision.

  New sub-routes rather than folding uploads into the existing JSON create/update endpoints (would have forced every existing numeric field through multipart string-coercion for no benefit, and touched already-working production code paths) — same pattern as `bikeIssue`'s pre-existing `/:id/status` sub-route: `PUT`/`DELETE .../fuel-logs/:id/image`, `PUT`/`DELETE .../maintenance-logs/:id/image`, `PUT`/`DELETE .../accessories/:id/image` (all `upload.single("image")`), and `POST .../issues/:id/images` (`upload.array("images", 5)`) / `DELETE .../issues/:id/images/:imageId` for the multi-image case. Every new service function reuses `findOwnedBikeOrThrow` — no new ownership-check logic anywhere; replacing a single-image field deletes the old Cloudinary asset first via `deleteCloudinaryImage`, and the bike-issue delete-by-`imageId` route finds the matching subdocument, destroys its Cloudinary asset, then filters it out of the array. No `router/index.ts` change needed (new routes inside already-registered `.route.ts` files).

  **Real pre-existing dependency bug found and fixed while getting `yarn build` clean**: `@types/multer` (`^1.4.12`, declared dependency `"@types/express": "*"`) was resolving to `@types/express@5.0.6` in a nested `node_modules/@types/multer/node_modules/@types/express`, while the rest of the project uses `@types/express@^4.17.21` (`4.17.25`) — a yarn-classic quirk where a `*` range doesn't get deduped against an already-resolved `^4.17.21` even though the resolved version would satisfy it. This produced confusing `router.delete(...)` overload-resolution errors across every route file that used the new `upload` middleware (Express-4-typed handlers next to an Express-5-typed one broke the whole overload set, not just the mismatched one). Fixed with a `"resolutions": { "@types/express": "^4.17.21" }` pin in `package.json` + `yarn install` — confirmed the nested duplicate is gone and `yarn build` is clean. This fix was necessary (not optional scope creep) because spec 17 was the first real usage of the already-installed-but-previously-unused `@types/multer`.

  Added new Postman requests (`Upload/Replace Receipt Image` + `Delete Receipt Image` under **Fuel Logs**; `Upload/Replace Service Image` + `Delete Service Image` under **Maintenance Logs**; new **Bike Accessories** folder with `Upload/Replace Product Image` + `Delete Product Image`; new **Bike Issues** folder with `Add Bike Issue Images` (auto-captures `bikeIssueImageId` from the last-added image) + `Delete Bike Issue Image`) to `postman/bikelog-api.postman_collection.json`, plus new collection variables `bikeAccessoryId`/`bikeIssueId`/`bikeIssueImageId`. Flagged, not fixed (pre-existing gap, not introduced by this spec): neither `bikeAccessory` nor `bikeIssue` has a `Create`/`List` request anywhere in this collection despite both modules being fully implemented since specs 10/11, so their id variables aren't auto-captured — documented in `dummy-data.md`'s new "Image uploads" section as something to paste in manually. Also removed the now-stale `dist/app/util/SendImageCloudinary.js` (orphaned build output for the deleted source file) while rebuilding `dist/`.

  Verified: `yarn build` clean; `yarn lint` shows only the same 4 pre-existing repo-wide errors, plus one new `no-console` warning in `cloudinary.ts` (consistent with the existing warning-tolerated pattern in `openRouterClient.ts`/the seed scripts, not an error). Extensively exercised live against a temporary local server instance and two throwaway registered users/bikes (not just a code read): uploaded a receipt image to a real fuel log, confirmed `receiptImage.url`/`publicId` on `GET`; replaced it and confirmed via a direct HTTP request to the _old_ Cloudinary URL that it now 404s (proving `deleteCloudinaryImage` actually destroyed the asset, not just overwrote the DB field) while the new one resolves; a non-image `.txt` upload attempt was rejected with a proper `400` (not `500`) before ever reaching Cloudinary; a second real user (owns a different bike) got `404` attempting to upload/delete/access another user's fuel log, maintenance log, or accessory image, and `404` adding images to another user's bike issue — ownership enforcement holds on every new route. Repeated the same upload/replace/delete/ownership pattern for `maintenanceLog.serviceImage` and `bikeAccessory.productImage` (including a double-delete on an already-imageless record correctly 404ing). For `bikeIssue.images[]`: added 2 images in one request, then 1 more in a second request (confirmed all 3 persist with distinct `_id`s); deleted the middle one by its `imageId` and confirmed exactly that one was removed from both the array and Cloudinary (verified via direct HTTP to its old URL, now 404) while the other two were untouched; a delete with a well-formed but nonexistent `imageId` correctly 404'd. All throwaway fixtures (2 users, 2 bikes, 1 fuel log, 1 maintenance log, 1 maintenance type, 1 accessory, 1 issue, and every uploaded test image) were fully cleaned up afterward — the two throwaway users and their bikes were hard-deleted directly against the database (via a temporary script reusing the app's own `config`/`mongoose.connect` — not a hand-built connection string) since no user-delete API endpoint exists; the accessory/maintenance-type/issue/log records and every uploaded image were removed through the app's own delete endpoints first so their Cloudinary assets were properly destroyed, not just detached from now-deleted parent documents.

- **2026-07-26 — Spec 16 (AI integration: spending insight, mileage insight, bike chat) implemented, scope limited strictly to spec 16.** New `ai` module at `/bikes/:bikeId/ai`, reusing `src/app/util/openRouterClient.ts`'s existing `askOpenRouter` choke point unchanged (no new client, no new fallback logic, no new dependency/env var). One small cleanup bundled per the spec: uncommented `openRouterClient.ts`'s `defaultHeaders`, set `"X-Title": "Bike Log"` and a placeholder `"HTTP-Referer"` (no deployed `bikelog_client-web-` URL exists yet). Added four optional cache fields to `TBike`/`bikeSchema` (`aiSpendingInsight`/`aiSpendingInsightLogCount`, `aiMileageInsight`/`aiMileageInsightFuelLogCount`, no `required`/`default`). `GET /bikes/:bikeId/ai/spending-insight` and `GET .../ai/mileage-insight`: count-based cache (fuel+maintenance log count for spending, fuel-log-only count for mileage) — regenerates only when the relevant count changes since the last cached run, not on a TTL; a bike with zero relevant logs returns a fixed "no data yet" message with `generated: false` and never calls the AI; mileage insight optionally folds in spec 15's `getMileageTrendFromDB(bikeId, 3)` for richer context but doesn't hard-depend on it. `POST /bikes/:bikeId/ai/chat`: stateless per request (client resends full history), `ai.validation.ts` rejects any `role !== "user" | "assistant"` (blocks a client-supplied `"system"` message — the actual system prompt is always server-constructed, never client-influenced), context bounded to the 20 most recent fuel logs + 20 most recent maintenance logs (`.sort({date:-1}).limit(20)`) plus lifetime spending totals, with an explicit "answer only from the data given, say so honestly if it doesn't cover the question" instruction in the system prompt. Ownership (`findOwnedBikeOrThrow`) checked inside each of the three service functions (matching `spending`'s convention, picked for consistency across this one new module rather than mixing `spending`'s and `mileageRecord`'s different existing conventions). Wired into `router/index.ts`; added an `AI` folder with all three requests to `postman/bikelog-api.postman_collection.json`. Verified: `yarn build` clean; `npx eslint` on all touched/new files clean (only the pre-existing `openRouterClient.ts` `no-console` warning); full `yarn lint` shows only the same 4 pre-existing repo-wide errors, none new. Extensively exercised live against real data in the dev DB (not just a code read): timed first-vs-cached calls for both insight endpoints (~16.6s → ~0.2s and ~5.5s → ~0.2s respectively — the ~80x speed gap is direct evidence the cache hit skipped the AI call, not just a response-shape check) with identical cached insight text; confirmed inserting a new fuel log invalidates the cache and regenerates a different insight; confirmed a zero-log throwaway bike returns the fixed message in milliseconds (not the multi-second AI round-trip) for both insight endpoints; confirmed the chat context cap holds exactly (30 inserted fuel logs on a temp bike → exactly 20 fetched by the same query `getBikeChatReply` uses); confirmed real chat replies are grounded in the actual fetched data (asked "how much have I spent" and got back the real lifetime total). Also drove all three endpoints over real HTTP against a temporary local server instance with a minted JWT: `role:"system"` in the chat body → `400`; empty `messages` → `400`; no auth → `401`; all three endpoints against a bike owned by a different real user in the dev DB → `404` in milliseconds (ownership check short-circuits before any AI call). The `FREE_MODELS`-exhausted → `AppError(503,...)` path was not re-exercised live (that fallback logic is pre-existing/untouched by this spec, and every new service function calls `askOpenRouter` without a wrapping `try/catch`, so any `AppError` it throws propagates unmodified through the same `catchAsync`/`globalErrorHandler` path already proven correct elsewhere in this codebase). All temporary verification scripts and throwaway DB fixtures (bikes/fuel logs) created for this pass were deleted/cleaned up afterward; one residual: the real dev-DB bike used for the cache-timing checks now has a stale `aiSpendingInsightLogCount` a script briefly bumped and then reverted the log count under — harmless, since the count-mismatch just means its cached insight will regenerate once more on next real use, not incorrect behavior.

- **2026-07-26 — Spec 15 (spending & mileage trend endpoints) implemented, scope limited strictly to spec 15.** New `GET /bikes/:bikeId/spending-summary/trend?months=N` and `GET /bikes/:bikeId/mileage/trend?months=N` (N optional, default `3`, validated `1–24` inline — same pattern as this module's existing month/year query-param validation), returning `{ months, monthlySummary: [...] }` rolling backward from the current month (not calendar-year-bound). `spending.service.ts`: extracted the existing fuel-log/maintenance-log find-and-reduce logic out of `getSpendingSummaryFromDB` into a new `computeSpendingForRange(bikeId, startDate?, endDate?)` helper (pure refactor, output unchanged), then added `getSpendingTrendFromDB(bikeId, userId, months)` which loops the helper over the last N months (ownership check inside the service, matching this module's existing convention). `mileageRecord.service.ts`: no refactor needed — added `TrendMileageResult` interface and `getMileageTrendFromDB(bikeId, months)`, reusing the already-existing `computeMileageForRange` helper in the same rolling-window loop (ownership check stays in the controller, matching `getYearlyMileage`'s existing shape — the two modules' pre-existing, different ownership-check conventions were deliberately left as-is per the spec). Added matching controller handlers (`getSpendingTrend`/`getMileageTrend`) and routes (`GET .../trend`) to both modules — no `router/index.ts` change needed, both are sub-paths of the already-mounted routers. Added `Mileage Trend`/`Spending Trend` requests to `postman/bikelog-api.postman_collection.json`. Verified: `yarn build` clean; `npx eslint` on both modules clean; full `yarn lint` shows only the same 4 pre-existing repo-wide errors, none new. Directly exercised both new service functions (not just a raw DB query) against a live bike from the dev DB: confirmed `getSpendingSummaryFromDB`'s `month`/`year`/`lifetime` outputs are unchanged after the `computeSpendingForRange` extraction; `months=3` (default) and `months=14` (crossing the 2025→2026 boundary) both returned the correct rolling window with exactly N entries, including zero-valued (not omitted) entries for months with no logs; `getMileageTrendFromDB`'s current-month entry matched `getYearlyMileageFromDB`'s output for the same month exactly, confirming `computeMileageForRange` reuse is correct. Also spun up a temporary local server instance (isolated port, not the possibly-running dev instance) and drove both new endpoints over real HTTP with a minted JWT: confirmed default `months=3`, `400` (not `500`) on `months=0`/`25`/`100`/`abc`, `401` with no auth header, and `404` (not another user's data) when requesting either endpoint for a bike owned by a different real user in the dev DB — direct IDOR check, not just a code read. No temporary scripts left behind (both removed after use).

- **2026-07-23 — Spec 14 (`bikeAccessory` optional `price`) implemented, scope limited strictly to spec 14.** Added `price?: number` to `TBikeAccessory` (`bikeAccessory.interface.ts`) and a matching `price: { type: Number }` schema field (`bikeAccessory.model.ts`, no `required`/`default`). Added `price: z.number().positive().optional()` to both `createBikeAccessorySchema` and `updateBikeAccessorySchema` (`bikeAccessory.validation.ts`), matching `fuelLog.validation.ts`'s existing convention for cost-like fields. No changes to `bikeAccessory.service.ts`/`.controller.ts`/`.route.ts` — `createBikeAccessoryIntoDB`'s payload spread and `updateBikeAccessoryInDB`'s generic `Object.assign` already pass the new optional field through with zero extra code, exactly as the spec predicted. `spending.service.ts` deliberately left untouched (out of scope per spec 14). Verified: `yarn build` clean; `npx eslint src/app/modules/bikeAccessory` clean. Directly exercised `bikeAccessoryServices.createBikeAccessoryIntoDB`/`updateBikeAccessoryInDB`/`getBikeAccessoriesFromDB` (not just a raw DB query) against a live bike from the dev DB: create without `price` leaves it `undefined`, create with a valid positive `price` persists it, Zod correctly rejects both `price: 0` and a negative `price`, `PATCH` can add a `price` to an accessory that didn't have one, and the list endpoint still works normally over the mix of priced/priceless accessories.

- **2026-07-23 — Wrote 1 new implementation-plan spec (`14-bike-accessory-price.md`), per direct user request.** Docs only, no source files touched. Design: add a plain optional `price?: number` to `TBikeAccessory`/`bikeAccessory.model.ts` (no `required`, no `default`), validated in `bikeAccessory.validation.ts` with `z.number().positive().optional()` (matching `fuelLog.validation.ts`'s existing convention for cost-like fields). No service/controller/route changes needed — `createBikeAccessoryIntoDB`'s payload spread and `updateBikeAccessoryInDB`'s generic `Object.assign` already pass an optional field through with zero extra code. Explicitly out of scope: wiring accessory price into `spending.service.ts`'s totals (flagged as a possible follow-up only). User wants to review the spec before authorizing implementation — no code changes made in this pass.

- **2026-07-23 — Spec 13 (`bikeAccessory` list sort order) implemented, scope limited strictly to spec 13, using a migration-free design from the start (informed directly by the spec-12 correction above).** The spec's original draft proposed the same `statusRank`+`pre("save")`-hook pattern spec 12 first shipped and then reverted — but unlike `bikeIssue`'s `open`/`resolved`, `AccessoryStatus`'s desired order (`pending → purchased → cancelled`) does **not** match alphabetical order (`cancelled < pending < purchased`), so spec 12's "just sort on the string field" fix doesn't transfer here; a real derived ordering is needed. Per direct user instruction ("query based on the status", no script), rewrote `getBikeAccessoriesFromDB` (`bikeAccessory.service.ts`) to run one plain `bikeAccessoryModel.find()` per status value (in `Object.values(AccessoryStatus)` order, or just the client's single requested status if `?status=` is given), each sorted by the client's `?sort=` (default `-createdAt`) and field-projected (default `-__v`, matching `Queryuilder`'s exact fallback semantics), then concatenates the three per-status arrays in fixed order and applies `.slice(skip, skip + limit)` over the merged list for pagination (replacing `Queryuilder`'s single-query `.skip()/.limit()` for this one function, since correctness across the merged groups requires it). `meta` is `bikeAccessoryModel.countDocuments()` over the same effective filter. No `statusRank` field, no `pre("save")` hook, no schema/interface change, no backfill script — nothing persisted or derived, so nothing can go stale for pre-existing documents. `bikeAccessory.model.ts`/`bikeAccessory.interface.ts`/`bikeAccessory.validation.ts`/`updateBikeAccessoryInDB` all left untouched (out of scope, no `statusRank` to strip since it was never added). `context/specs/13-bike-accessory-list-sort-order.md` rewritten in place to document this design instead of the original `statusRank` draft. Verified: `yarn build` clean; `npx eslint src/app/modules/bikeAccessory` clean. Directly exercised `bikeAccessoryServices.getBikeAccessoriesFromDB` (not just a raw DB query) against real fixtures inserted with deliberately out-of-order `createdAt` timestamps on a live bike from the dev DB: confirmed default order is pending (newest-first) → purchased → cancelled; `?sort=name` still reorders within each group; `?limit=1` walked one page at a time across 6 fixtures produced one continuous pending→purchased→cancelled sequence with no per-page reordering; an explicit `?status=purchased` filter correctly returned only that status. Not separately re-verified via the live HTTP layer in this pass: enum-rejection at the validation layer and cross-bike/IDOR isolation (both unchanged code paths, not touched by this rewrite).

- **2026-07-23 — Spec 12 corrected: `statusRank` approach reverted in favor of sorting directly on `status`, scope limited strictly to spec 12.** The 2026-07-22 implementation below had a real migration gap: `statusRank` was only kept correct by a `pre("save")` hook, so any `BikeIssue` already in the DB before that fix shipped kept `statusRank: 0` regardless of actual status until individually re-saved — confirmed against live dev data (a `resolved` issue reported 2026-07-20 was still sorting before an `open` issue reported 2026-07-01). Per direct user instruction, no backfill/migration script — instead reverted to a simpler fix: sort directly on the existing `status` string field (`"open" < "resolved"` lexicographically, so `.sort("status -dateReported")` already produces open-before-resolved with zero persisted state and zero migration risk). Removed `statusRank` entirely as dead code: dropped from `TBikeIssue` (`bikeIssue.interface.ts`), dropped the schema field + `pre("save")` hook from `bikeIssue.model.ts`, dropped the `delete updateData.statusRank` line from `updateBikeIssueInDB` (`bikeIssue.service.ts`, field no longer exists so nothing to strip), changed `getBikeIssuesFromDB`'s sort from `.sort("statusRank -dateReported")` to `.sort("status -dateReported")`. Left the 2026-07-22 `updateBikeIssueStatus` fetch-then-`.save()` refactor in place (still correct module convention, independent of `statusRank`). `context/specs/12-bike-issue-list-sort-order.md` rewritten in place to document the revised design (superseding, not appending to, the original doc). Verified: `yarn build` clean; `npx eslint src/app/modules/bikeIssue` clean; full `yarn lint` shows only the same 4 pre-existing repo-wide errors, none new. Ran a read-only script directly against the live dev Atlas DB (no writes) comparing old (`-dateReported` only, i.e. what stale-`statusRank` docs effectively fall back to) vs. new (`status -dateReported`) sort on a bike with pre-existing mixed open/resolved issues — confirmed the new sort immediately fixes the ordering with no script/backfill run, proving the migration gap is fully closed.

- **2026-07-22 — Spec 12 (`bikeIssue` list sort order) implemented, scope limited strictly to spec 12.** Added `statusRank: number` to `TBikeIssue`/`bikeIssue.model.ts` (schema field, `default: 0`), kept in sync via a new `bikeIssueSchema.pre("save")` hook computing `Object.values(BikeIssueStatus).indexOf(this.status)` (open=0, resolved=1). `getBikeIssuesFromDB`'s sort changed from `.sort("-dateReported")` to `.sort("statusRank -dateReported")`. Bundled real bug fix: `updateBikeIssueStatus` previously called `bikeIssueModel.findByIdAndUpdate(...)`, which bypasses `pre("save")` document middleware entirely — rewrote it to the fetch-then-mutate-then-`.save()` pattern already used by every other mutator in the module (`issue.status = status; await issue.save();`), matching module convention and letting the hook fire on status flips. Added `delete updateData.statusRank` in `updateBikeIssueInDB` alongside the existing `delete updateData.status` so the field can't be spoofed via generic PATCH. No validation-schema changes (by design — `statusRank` is server-derived only). Skipped the spec's optional backfill script (`backfillBikeIssueStatusRank.ts`) as out of scope for this pass. Verified: `yarn build` clean; `npx eslint src/app/modules/bikeIssue` clean; full `yarn lint` shows only the same 4 pre-existing repo-wide errors, none new. Manually smoke-tested end-to-end against an isolated local instance (the already-running dev server on port 5000 hadn't picked up the change, so a temporary instance was started on port 5099 specifically to verify against current code): confirmed open-before-resolved ordering independent of `dateReported` (including a case where a resolved issue was reported _after_ an open one), `-dateReported` descending order preserved as the secondary key within each status group, `statusRank` correctly flips 0→1 on `PATCH /:id/status` (proving the `.save()` fix actually re-triggers the hook), the already-resolved 400 guard still works, a spoofed `statusRank` in a generic `PATCH /:id` body has zero effect, pagination (`?limit=1` across 3 pages) reflects one continuous open-before-resolved order rather than a per-page reorder, and cross-bike/IDOR isolation (including a spoofed `?bike=<otherBikeId>` query param) remains intact.

- **2026-07-22 — Wrote 2 new implementation-plan specs (`12-bike-issue-list-sort-order.md`, `13-bike-accessory-list-sort-order.md`), per direct user request.** Docs only, no source files touched. Design: add a materialized `statusRank` numeric field to each model (derived from the enum's own declared order via `Object.values(...).indexOf(status)`), kept in sync by a `pre("save")` hook (precedent: `user.model.ts`'s password-hashing hook), sorted with a compound Mongoose sort string (`"statusRank -dateReported"` / `"statusRank -createdAt"`) — rejected an in-memory JS sort (would break across paginated pages) and an aggregation pipeline (`Queryuilder` only wraps `Query`, never `Aggregate`; would be the first `.aggregate()` call in the codebase, against the documented house-style aversion to aggregation pipelines). Spec 12 also bundles a real bug fix: `updateBikeIssueStatus` currently uses `findByIdAndUpdate`, which bypasses `pre("save")` document middleware entirely, so it's rewritten to the fetch-then-mutate-then-`.save()` pattern already used elsewhere in the module. Spec 13 flags a regression risk unique to `bikeAccessory`: its list endpoint currently honors a client `?sort=` via `Queryuilder`'s fallback (unlike `bikeIssue`, which hardcodes its sort), so the fix must compose `statusRank` with the client's existing sort value rather than hardcoding it away.

- **2026-07-21 — Spec 11 (`bikeAccessory`) implemented, per direct user request, scope limited strictly to spec 11.** New module at `/bikes/:bikeId/accessories`, matching the design exactly: `bikeAccessory.constant.ts` (`AccessoryUrgency` — `immediate`/`medium`/`low` — and `AccessoryStatus` — `pending`/`purchased`/`cancelled` — both `as const`), `bikeAccessory.interface.ts` (`TBikeAccessory`: `bike`, `name`, `urgency`, `status`, `isDeleted`), `bikeAccessory.model.ts` (both `urgency`/`status` enum-validated at the Mongoose level via `enum: Object.values(...)`, `status` defaults to `pending`, soft-delete `pre("find")`/`pre("findOne")` hooks), `bikeAccessory.validation.ts` (`createBikeAccessorySchema`: `name`+`urgency` required, `status` optional; `updateBikeAccessorySchema`: all optional — both zod-enum-validated against the same constant values), `bikeAccessory.service.ts` (5 plain CRUD functions, no dedicated action endpoints since no field here is derived or has a side effect; `getBikeAccessoriesFromDB` strips `bike`/`isDeleted` from the query before `QueryBuilder` — same IDOR-safe pattern as `fuelLog`/`maintenanceLog`/`bikeIssue`; list sort uses `QueryBuilder`'s default `-createdAt`, no explicit event-date field since none exists for a wishlist entry), `bikeAccessory.controller.ts` (5 thin `catchAsync` wrappers), `bikeAccessory.route.ts` (single `Router({ mergeParams: true })`, plain CRUD only — no bespoke endpoints, unlike `bikeIssue`). Wired into `router/index.ts` at `/bikes/:bikeId/accessories`. Verified: `yarn build` clean; `npx eslint src/app/modules/bikeAccessory src/app/router` clean; full `yarn lint` shows only the same 4 pre-existing repo-wide errors + pre-existing warnings, none new and none in `bikeAccessory`. Manual endpoint verification (via `postman`/live server) not yet run in this pass — spec 11's checklist items around enum rejection, filter combinations, and cross-bike isolation should be smoke-tested against a running server before this is treated as fully verified end-to-end, though the code mirrors `bikeIssue`'s already-verified ownership/IDOR/soft-delete plumbing exactly.

- **2026-07-21 — `bikeIssue` simplified to match actual usage.** Per direct user request/clarification of the real workflow ("log an issue → mark resolved when fixed → flip back to open if it recurs, nothing more"), corrected drift between the spec-10 design below and what was actually shipped: the model never had a `history` array despite `TBikeIssue`'s interface still declaring one (dead type, removed), the dedicated `PATCH /:id/reopen` endpoint was pure duplication of a validated `PATCH /:id/status`, and two real bugs were found and fixed — `updateBikeIssueStatusSchema` validated `status` as a bare `z.string()` (any string persisted, since `findByIdAndUpdate` doesn't run schema validators by default) and `findByIdAndUpdate` was called without `{ new: true }`, so the response returned the pre-update document. Fixed: `bikeIssue.interface.ts` drops `TBikeIssueHistoryEntry`/`history`; `bikeIssue.validation.ts`'s status schema is now `z.enum(["open","resolved"])`; `bikeIssue.service.ts`'s `updateBikeIssueStatus` now 400s if the requested status equals the current one and calls `findByIdAndUpdate` with `{ new: true, runValidators: true }`; `reopenBikeIssueInDB` removed (its behavior — flipping back to `open` — is now just `PATCH /:id/status { status: "open" }`), and its controller/route/validation-schema removed with it. `context/specs/10-bike-issue.md` annotated with a correction note pointing at this entry. Verified: `yarn build` clean, `npx eslint src/app/modules/bikeIssue` clean.

- **Spec 10 — bike issue tracking** (original implementation note, now partially superseded by the 2026-07-21 entry above — the `history`/resolve-reopen design described here was not what ended up shipped): Implemented new `bikeIssue` module (a scope expansion beyond the original plan doc, per direct user request — see spec 10 for motivation). `bikeIssue.constant.ts` (`BikeIssueStatus` `as const`), `bikeIssue.interface.ts` (`TBikeIssueHistoryEntry`, `TBikeIssue`), `bikeIssue.model.ts` (sub-schema for `history` with `{ _id: false }`, soft-delete hooks), `bikeIssue.validation.ts` (create/update/resolve/reopen Zod schemas), `bikeIssue.service.ts` (7 functions: create forces `status: "open"`/`history: []`; `getBikeIssuesFromDB` strips `bike`/`isDeleted` from query before `QueryBuilder` — same IDOR-safe pattern as `fuelLog`/`maintenanceLog`; generic update strips `status`/`history`; `resolveBikeIssueInDB` pushes a new `history` entry with optional `resolvedInMaintenanceLog` validated against `maintenanceLogModel` scoped to the bike, rejects if already resolved; `reopenBikeIssueInDB` sets `reopenedAt` on the _last_ history entry, calls `markModified("history")` before `save()`, rejects if already open), `bikeIssue.controller.ts` (7 thin `catchAsync` wrappers), `bikeIssue.route.ts` (single `Router({ mergeParams: true })`, dedicated `PATCH /:id/resolve` and `PATCH /:id/reopen` alongside generic CRUD). Wired into `router/index.ts` at `/bikes/:bikeId/issues`. Verified: `yarn build` clean, `yarn lint` shows only the 4 pre-existing repo-wide errors (none in `bikeIssue`); full manual smoke test against a running server covered every item in spec 10's Verify-when-done checklist — forced `open`/`[]` on create despite client-sent `status`/`history`, resolve→reopen→resolve cycle produced exactly 2 `history` entries with `reopenedAt` correctly set only on the entry it belongs to, double-resolve and double-reopen both 400, generic `PATCH /:id` left `status`/`history` untouched while updating `title`, a bogus `resolvedInMaintenanceLog` id 404'd, `?status=` filtering worked, and `?bike=<otherBikeId>` query-string IDOR + cross-bike `GET` both stayed correctly scoped to the URL's `:bikeId`.

- **Full audit of specs 02–09 against their implementations** (all 8 modules, per the user's request to go through every implementation plan and fix any bugs found). Found and fixed 4 real issues:
  - **Security (IDOR): `getFuelLogsFromDB` and `getMaintenanceLogsFromDB`** passed raw `req.query` straight into `QueryBuilder.filter()`, which runs a second `.find(queryObj)` call on top of the ownership-scoped `.find({ bike: bikeId, isDeleted: false })`. Mongoose merges conflicting keys with last-write-wins, so an authenticated user who owns _some_ bike could hit `GET /bikes/:bikeId/fuel-logs?bike=<anotherUsersBikeId>` (or the equivalent on maintenance-logs) and read another user's data — `findOwnedBikeOrThrow` only ever checked the URL's `:bikeId`, not the one smuggled through the query string. Verified experimentally against the installed Mongoose version before fixing. Fixed in both services by stripping `bike`/`isDeleted` from the query object before it reaches `QueryBuilder`.
  - **Security/correctness: `user.services.ts`'s `createUser`** never applied the `.select("-password")` that spec 02 called for (progress-tracker previously claimed this was done — it wasn't) — `POST /api/auth/register` was leaking the argon2 hash in its response. Fixed to re-fetch with `.select("-password")`, and closed the long-flagged duplicate-email gap in the same pass (see below) by adding the same manual 11000-catch → 409 pattern already used in `maintenanceType`/`engineOilType`.
  - **Correctness: `fuelLog.service.ts`'s `updateFuelLogInDB`** only recomputed `totalCost` when `litersAdded`/`pricePerLiter` changed, but never stripped a client-submitted `totalCost` otherwise — a bare `PATCH .../fuel-logs/:id` with just `{ totalCost: <anything> }` would persist verbatim, violating the "totalCost is always server-derived" invariant. Fixed by unconditionally deleting `payload.totalCost` before the recompute check.
  - **Correctness: `spending.service.ts`** filtered maintenance categories to `total > 0` before building `categoryBreakdown`, which would silently drop a legitimately-logged $0-cost maintenance entry from the breakdown — contradicts spec 09's "one per distinct maintenance type actually logged in the window" rule. Fixed by removing the filter (the map only ever contains categories with at least one log anyway).
  - Reviewed and found **no issues** in: `bike` (spec 03), `mileageRecord` (spec 05), `maintenanceType` (spec 06), `engineOilType` (spec 07), the rest of `maintenanceLog` (spec 08, referential checks/`nextDueOdometer`/reminder buffers all correct), and core plumbing (`app.ts`, `router/index.ts`, `AppError`/`catchAsync`/`sendResponse`, `globalErrorHandler.ts`).
  - Verified: `yarn build` clean; `npx eslint` on all touched modules shows zero new errors (only the same pre-existing `user.model.ts` `no-this-alias` error, unrelated to this change).
  - **Previously-flagged duplicate-email gap (now resolved):** the note below from the specs 06–07 review — "registering with an existing email currently returns a 400 with a raw Mongo error string" — is fixed as part of this pass.

- **Review of specs 06–07** (`maintenanceType`, `engineOilType` — already implemented, per the user's request to check them). No bugs found in either module: `createXIntoDB`/`getXFromDB`, models, validation, and routes all match their specs exactly. This also resolved the one open question spec 06 itself flagged ("this duplicate-key catch may be redundant, confirm during implementation") — traced through `globalErrorHandler.ts`: the codebase-wide generic `error?.code === 11000` handler (`handleDuplicateError.ts`) returns **400** with the raw Mongo error string as the top-level `message` (not 409, not a clean message). So the manual try/catch in both services is **not redundant** — it's what upgrades a duplicate-name conflict from a leaky 400 to a clean `409 "...already exists"`. Confirmed via `yarn build` (clean) and `npx eslint` on both modules + `src/scripts` (0 errors, only expected `no-console` warnings in the seed scripts). No code changes needed.
  - **Flagged, not fixed (out of scope for 06/07):** the same generic 400-with-raw-message path is still what handles a duplicate **email** on `POST /auth/register` (`user.services.ts`'s `createUser` has no manual 11000 catch, unlike maintenanceType/engineOilType) — registering with an existing email currently returns a 400 with a raw `E11000 duplicate key error...` string as the message, not a clean 409. Same root cause, different module (spec 02, already implemented) — worth a follow-up if the user wants it addressed.

- **Bug-fix review of specs 03–05** (`bike`, `fuelLog`, `mileageRecord` — already implemented, per the user's request to "go through their implementation" and fix any issues found). Found and fixed:
  - `Bike` had no immutable "starting" odometer — only the denormalized `currentOdometer`, which every fuel/maintenance log bumps. Added a new `initialOdometer` field (snapshot of `currentOdometer` at creation, never changed after), set in `bike.service.ts`'s `createBikeIntoDB` and stripped from `updateBikeInDB`'s payload like `owner`/`currentOdometer`.
  - `fuelLog.service.ts`'s mileage-closure fallback (no prior full-tank fill) was reading `bike.currentOdometer` **after** `bumpOdometerIfHigher` had already mutated it to equal the new fill's own reading — this collapsed `distanceKm` to 0 for every bike's very first `MileageRecord`. Fixed to anchor on the new immutable `bike.initialOdometer` instead.
  - Same file's period-liters query used `$gte: periodStartDate` instead of `$gt`, double-counting the previous closing full-tank fill's own liters into the next period's `litersConsumed` (understating every mileage figure after the bike's first period). Fixed to `$gt`.
  - `mileageRecord.service.ts`: `computeMileageForRange`'s no-prior-log fallback had the same `currentOdometer`-vs-`initialOdometer` bug as above (wrong for a bike's first month/year of data once any later log had bumped `currentOdometer`) — fixed. `getLifetimeMileageFromDB` anchored on the earliest fuel log's reading rather than the bike's true starting point, undercounting any km ridden before the first-ever fuel log — fixed to use `initialOdometer`.
  - `getMileageRecordsFromDB`'s "approximate" rolling-average was computed by averaging existing `MileageRecord`s — which never exist for a user who never does a full-tank fill, making the fallback permanently non-functional for exactly the case spec 05/the plan doc built it for. Rewrote to compute the rolling average from the trailing 10 raw `FuelLog`s (regardless of `isFullTank`), matching spec 05; response shape changed from `{records, approximateMileage}` to `{exactRecords, approximate: {mileageKmPerLiter, basedOnFuelLogCount, isEstimate} | null}`.
  - `validateRequest.ts` called `Schema.parseAsync()` but discarded the result instead of reassigning it to `req.body` — meaning zod's default unknown-key-stripping never actually reached the controller/service layer, so extra client-supplied fields (e.g. a spoofed `isDeleted` or `owner` in a request body) passed through unstripped. Fixed to assign `req.body = parsed.body`.
  - Verified: `yarn build` clean, `npx eslint` clean on all touched files (`bike`, `fuelLog`, `mileageRecord`, `validateRequest.ts`).

- Implemented [`01-module-scaffolding-and-models.md`](specs/01-module-scaffolding-and-models.md) in full, per the user's explicit request. `yarn install` (node_modules wasn't present), `yarn build` clean, `yarn lint` clean for every new/touched module and `router/index.ts` — verified in isolation (`npx eslint src/app/modules/{bike,fuelLog,mileageRecord,maintenanceType,engineOilType,maintenanceLog,spending,user} src/app/router`), only 1 pre-existing error surfaced there (`user.model.ts`'s `const user = this;` in the `pre("save")` hook, `@typescript-eslint/no-this-alias` — present before this change, not introduced by it, and out of scope per spec 01's "keep argon2 hashing untouched"). A full repo-wide `yarn lint` also shows 4 more pre-existing errors + 1 warning, all in files spec 01 doesn't touch: `app.ts` (unused `next` in the 404 handler), `Queryuilder.ts` (`prefer-const`), `interface/index.d.ts` (unused `Request` import), `globalErrorHandler.ts` (unused `next`), and a `no-console` warning in `openRouterClient.ts`. None were introduced by this change; fixing them wasn't part of spec 01's scope and wasn't done, per the user's explicit "don't go beyond spec 01" instruction.
- Wrote 8 new implementation-plan specs (`02` through `09`), one per module, covering the real business logic for the whole backend — per the user's explicit request to review each module's plan before any code is written. Docs only, no source files touched. Ground-truthed against the actual current stub code (not just the older plan doc) via direct file reads, surfacing a few real bugs/gaps along the way (see Known Gaps below).
- Implemented [`02-auth-hardening.md`](specs/02-auth-hardening.md): fixed password re-hash guard, added `TJwtPayload` type with `userRole` for forward-compat admin support, tightened `Request.user` type, centralized JWT expiry config, stripped password from register/login/me responses, renamed `crateUser` → `createUser`, added `GET /auth/me`. All verification steps pass: `yarn build` clean, `yarn lint` clean for touched files, endpoints verified (no password in responses, token includes `userRole: "user"`, re-hash guard functional).
- Implemented [`03-bike-crud.md`](specs/03-bike-crud.md): created `bike.utils.ts` with shared `findOwnedBikeOrThrow`/`bumpOdometerIfHigher` helpers; implemented all 5 bike service functions (create, list, get by id, update, soft delete) with ownership scoping via `req.user.userId`; updated controller with real handlers using `sendResponse`; exported `bumpOdometerIfHigher` for reuse by `fuelLog`/`maintenanceLog` (specs 04/08). `yarn build`/`lint` clean.
- Implemented [`04-fuel-log-and-mileage-closure.md`](specs/04-fuel-log-and-mileage-closure.md): made `totalCost` optional in validation (server computes `litersAdded * pricePerLiter`); `createFuelLogIntoDB` runs ownership check via `findOwnedBikeOrThrow`, computes `totalCost`, saves `FuelLog`, bumps bike odometer via `bumpOdometerIfHigher`, runs full-tank closure (finds previous full-tank log or uses bike creation, sums liters in period, creates `MileageRecord`); `getFuelLogsFromDB` uses `QueryBuilder` with `-date` default sort + pagination; `updateFuelLogInDB`/`deleteFuelLogFromDB` return 409 if referenced by `MileageRecord`. `yarn build`/`lint` clean.
- Implemented [`05-mileage-stats.md`](specs/05-mileage-stats.md): implemented 4 read-only endpoints (`getMileageRecords`, `getMonthlyMileage`, `getYearlyMileage`, `getLifetimeMileage`) with shared `computeMileageForRange` helper; exact `MileageRecord` history + live rolling-average fallback (trailing 10 fuel logs); monthly/yearly/lifetime totals bucketed by `FuelLog.date` (not `createdAt`); inline validation of `targetMonth`/`targetYear` query params returns 400 on bad input; no average fields returned (client computes). `yarn build`/`lint` clean.
- Implemented [`06-maintenance-type-catalog.md`](specs/06-maintenance-type-catalog.md): implemented `createMaintenanceTypeIntoDB` with duplicate-key handling (catches Mongo code 11000 → 409 `AppError`), `getMaintenanceTypesFromDB`; created `seedMaintenanceTypes.ts` + `yarn seed:maintenance-types` — idempotent upsert-by-name for 8 plan types (Engine Oil, Chain Lube 500km, Tire Change, Brake Pads, General Service 3000km, Insurance 365d, Registration/Tax 365d, Other). `yarn build`/`lint` clean.

## Known Gaps / Open Questions

- No automated test suite (`yarn test` is a stub) — inherited from the boilerplate, not yet decided whether to add one.
- 5 pre-existing lint errors + 1 warning remain repo-wide (see Recent Activity above) — none in Bike Log modules, all predate this change, none fixed since it was out of spec 01's scope. Worth a dedicated cleanup pass before this goes live.
- Exact "upcoming" reminder buffer was locked to 50km in spec 08's implementation (the plan doc's example value, explicitly confirmed in the spec).
- `engine-oil-types` and `maintenance-types` endpoints currently have no seed data — [`06-maintenance-type-catalog.md`](specs/06-maintenance-type-catalog.md) and [`07-engine-oil-type-catalog.md`](specs/07-engine-oil-type-catalog.md) both propose seed scripts with specific values, but those values (especially oil-change intervals) are flagged as assumptions needing confirmation, not researched numbers.
- `spending`'s response interface (`TSpendingSummary`) only had a `period` field with no way to select which month/year — [`09-spending-summary.md`](specs/09-spending-summary.md) resolved this by adding `targetMonth`/`targetYear` query params (implemented).
- Neither `bikeAccessory` nor `bikeIssue` has a `Create`/`List` request in `postman/bikelog-api.postman_collection.json`, despite both modules being fully implemented since specs 10/11 — pre-existing gap, not introduced or fixed by spec 17 (which added `bikeAccessoryId`/`bikeIssueId` collection variables for its own new image-upload requests, but left backfilling full CRUD coverage for those two modules out of scope). Worth a follow-up if manual Postman testing of those modules' base CRUD is wanted.
- `bikeIssue`'s pre-existing `POST .../issues/:id/images` route (`upload.array("images", 5)`, spec 17) returns a raw `500`, not a clean `400`, if a client sends more than 5 images in one request — `multer.MulterError` isn't an `AppError` and `globalErrorHandler` has no special-case handling for it. Discovered while implementing/verifying spec 19's own equivalent cap, which was fixed there with a route-local wrapper (see spec 19's Recent Activity entry) rather than a shared `globalErrorHandler.ts` fix. Not fixed on `bikeIssue` itself (out of scope for spec 19) — worth a small follow-up (either the same route-local wrapper pattern, or a proper `instanceof multer.MulterError` branch in `globalErrorHandler.ts` if this turns out to affect more than one route).

## Next Up

All 8 original Bike Log modules (specs 02–09), the spec-10 `bikeIssue` addition, the spec-11 `bikeAccessory` addition, the spec-15 spending/mileage trend endpoints, the spec-16 AI integration module, the spec-17 Cloudinary image-upload feature, the spec-18 bike-manual AI grounding feature, the spec-19 bike-documents feature (all v2 scope past spec 15), the spec-20 decimal-number-field audit, the spec-21 weekly-summary push notification module, the spec-22 trend-endpoint 6-month re-verification, and the spec-23 spending-details export endpoint are implemented (specs 20 and 22 both required no service/controller/route code change — see their own Recent Activity entries). **All 23 backend specs are now complete — no remaining MVP or post-MVP-scope-expansion backend work.** Spec 23's own consumer, `bikelog_client-web-`'s spec 21 (Spending PDF Export), is not this project's work but can now proceed against a live endpoint. Per direct user instruction, spec 18 and spec 19 both have **no frontend counterpart implemented yet** — spec 19's own client-side implementation plans already exist (`bikelog_client-web-/context/specs/17-bike-documents.md`, `bikelog_app/ai context/specs/22-bike-documents.md`), written in an earlier pass but not yet built. Specs 20 and 21's own sibling frontend work, by contrast, **is** already built (`bikelog_client-web-/context/specs/19-decimal-number-field-fix.md`, `bikelog_app/ai context/specs/23-decimal-number-field-fix.md` for spec 20; `bikelog_app/ai context/specs/24-weekly-bike-summary-notification.md` for spec 21 — this app-only feature has no web counterpart, per the original spec's own scope). **Outstanding, outside-this-repo steps for spec 21 to actually run in production**: add `CRON_SECRET` to the deployed Vercel project's env vars, and add `API_BASE_URL`/`CRON_SECRET` as GitHub Actions repository secrets so `.github/workflows/weekly-summary-cron.yml` can actually fire — neither was done as part of this pass (deliberately not touching `.env`/external dashboards). Next steps would be frontend integration of the trend endpoints, the `ai` module, spec 18's manual upload UI, and spec 19's bike-documents UI in `bikelog_client-web-`/`bikelog_app` (their own specs, not this project's), `bikeIssue`/`bikeAccessory` domain hooks/UI (neither yet built on any client), backfilling `bikeAccessory`/`bikeIssue` CRUD requests into the Postman collection (see Known Gaps), the `bikeIssue` `MulterError`-500 follow-up noted above, pre-existing lint cleanup, spec 20's two optional (not yet requested) hardening items, spec 21's optional Expo push-receipt-checking follow-up, or further v2 features (`v2-proposed-features/`).
